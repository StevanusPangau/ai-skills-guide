export const BROOKLYN_SOURCE_REPO = 'github.com/OutThisLife/brooklyn-skills'
export const BROOKLYN_SOURCE_SHA = '037ad3ca8efc0f4fce9d34346e969966b96e57df'

export type BilingualString = { id: string; en: string }
export type BilingualList = { id: string[]; en: string[] }

export type RichSkill = {
  name: string
  category: string
  invocation: 'user' | 'model'
  description: BilingualString
  detailedDescription: BilingualString
  useWhen: BilingualList
  avoidWhen: BilingualList
  howItWorks: BilingualList
  coreRules: BilingualList
  tips: BilingualList
  pairsWellWith: string[]
  sourcePath: string
}

export const brooklynSkills: RichSkill[] = [
  {"name": "audit-only", "category": "quality", "invocation": "model", "description": {"id": "Skill autonomous engineering Brooklyn untuk /audit-only.", "en": "Brooklyn autonomous engineering skill for /audit-only."}, "detailedDescription": {"id": "# Audit Only\n\nInvestigate and report. Do not edit code, create files, or open PRs until the\nuser explicitly says to proceed.\n\n## Do\n\n1. Read the relevant code/data and answer the actual question first.\n2. Report findings: what's there, what's wrong, options + a recommendation.\n3. If a change is warranted, describe it and wait for a go.\n4. If asked, post findings as a PR/issue comment — that's still not code edits.\n\n## Don't\n\n- Start writing code because the fix seems obvious.\n- Act on assumed scope — answer what was asked, nothing more.\n- Treat \"review this\" or a plain question as permission to change things.", "en": "# Audit Only\n\nInvestigate and report. Do not edit code, create files, or open PRs until the\nuser explicitly says to proceed.\n\n## Do\n\n1. Read the relevant code/data and answer the actual question first.\n2. Report findings: what's there, what's wrong, options + a recommendation.\n3. If a change is warranted, describe it and wait for a go.\n4. If asked, post findings as a PR/issue comment — that's still not code edits.\n\n## Don't\n\n- Start writing code because the fix seems obvious.\n- Act on assumed scope — answer what was asked, nothing more.\n- Treat \"review this\" or a plain question as permission to change things."}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /audit-only.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /audit-only.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /audit-only pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /audit-only protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["Start writing code because the fix seems obvious.", "Act on assumed scope — answer what was asked, nothing more.", "Treat \"review this\" or a plain question as permission to change things."], "en": ["Start writing code because the fix seems obvious.", "Act on assumed scope — answer what was asked, nothing more.", "Treat \"review this\" or a plain question as permission to change things."]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "clean", "no-tropes"], "sourcePath": "skills/audit-only/SKILL.md"},
  {"name": "babysit", "category": "quality", "invocation": "user", "description": {"id": "Skill autonomous engineering Brooklyn untuk /babysit.", "en": "Brooklyn autonomous engineering skill for /babysit."}, "detailedDescription": {"id": "# Babysit\n\nStay on the PR after handoff until it's actually done. Being asked to babysit\nIS the ask to poll — but report state changes only, no heartbeats, no\nper-poll narration.\n\n## Loop\n\n1. Snapshot: `gh pr view <N> --json\n   state,mergeable,mergeStateStatus,reviewDecision,statusCheckRollup,autoMergeRequest`\n   plus `gh pr checks <N>`.\n2. Poll inside bounded shell loops (a single `for` with `sleep 30`–`60`\n   between iterations), not one message per check.\n3. React by cause:\n   - **CI never started** → kick it:\n     `git commit --allow-empty -m \"chore: kick CI\" && git push`.\n   - **Red caused by this branch** → fix it (the `pr-ready` loop), push,\n     resume watching on the new SHA.\n   - **Flake / infra red** → rerun the failed jobs; repeat until green. Note\n     the flake in one line with the run link.\n   - **New review threads** → handle per `pr-ready` (fix or reply, verify\n     resolved via the forge API), resume.\n4. Repeat until done. A push or rerun is progress, not an exit.\n\n## Merging\n\nArm automerge only when the ask says so — \"set to automerge\", \"make CI\ngreen\", or any mention of merging. A plain \"babysit\" / \"watch it\" ends at\ngreen with threads resolved: report, don't merge.\n\n## CI quirks\n\n- `gh run rerun` refusing with \"already running\" → cancel the run first,\n  then rerun.\n- A cancelled run showing \"no checks reported\" is pending, not red — wait\n  for it to re-report before calling it either way.\n- Rollup gates (\"All required checks pass\") go red because a leaf did —\n  diagnose the leaf failure, never the gate.\n\n## Done when\n\nRe-query the forge — don't trust that a push went green or a rerun stuck.\nMerged, or every check green with no unresolved threads. Fail closed: any\nred or open thread means keep watching. Wrap up with the PR/MR link and what\nthe watch consumed — reruns used, kicks, fixes pushed.\n\n## Scope\n\n- **In:** watching an open PR, kicking stalled CI, rerunning flakes, small\n  branch-caused fixes, late review threads.\n- **Out:** opening or refreshing the PR (`pr-update`, `cpr`), full\n  rebase/red-CI overhauls (`pr-ready`), other people's PRs (`pr-triage`),\n  merging unasked.", "en": "# Babysit\n\nStay on the PR after handoff until it's actually done. Being asked to babysit\nIS the ask to poll — but report state changes only, no heartbeats, no\nper-poll narration.\n\n## Loop\n\n1. Snapshot: `gh pr view <N> --json\n   state,mergeable,mergeStateStatus,reviewDecision,statusCheckRollup,autoMergeRequest`\n   plus `gh pr checks <N>`.\n2. Poll inside bounded shell loops (a single `for` with `sleep 30`–`60`\n   between iterations), not one message per check.\n3. React by cause:\n   - **CI never started** → kick it:\n     `git commit --allow-empty -m \"chore: kick CI\" && git push`.\n   - **Red caused by this branch** → fix it (the `pr-ready` loop), push,\n     resume watching on the new SHA.\n   - **Flake / infra red** → rerun the failed jobs; repeat until green. Note\n     the flake in one line with the run link.\n   - **New review threads** → handle per `pr-ready` (fix or reply, verify\n     resolved via the forge API), resume.\n4. Repeat until done. A push or rerun is progress, not an exit.\n\n## Merging\n\nArm automerge only when the ask says so — \"set to automerge\", \"make CI\ngreen\", or any mention of merging. A plain \"babysit\" / \"watch it\" ends at\ngreen with threads resolved: report, don't merge.\n\n## CI quirks\n\n- `gh run rerun` refusing with \"already running\" → cancel the run first,\n  then rerun.\n- A cancelled run showing \"no checks reported\" is pending, not red — wait\n  for it to re-report before calling it either way.\n- Rollup gates (\"All required checks pass\") go red because a leaf did —\n  diagnose the leaf failure, never the gate.\n\n## Done when\n\nRe-query the forge — don't trust that a push went green or a rerun stuck.\nMerged, or every check green with no unresolved threads. Fail closed: any\nred or open thread means keep watching. Wrap up with the PR/MR link and what\nthe watch consumed — reruns used, kicks, fixes pushed.\n\n## Scope\n\n- **In:** watching an open PR, kicking stalled CI, rerunning flakes, small\n  branch-caused fixes, late review threads.\n- **Out:** opening or refreshing the PR (`pr-update`, `cpr`), full\n  rebase/red-CI overhauls (`pr-ready`), other people's PRs (`pr-triage`),\n  merging unasked."}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /babysit.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /babysit.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /babysit pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /babysit protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["CI never started** → kick it:", "Red caused by this branch** → fix it (the `pr-ready` loop), push,", "Flake / infra red** → rerun the failed jobs; repeat until green. Note", "New review threads** → handle per `pr-ready` (fix or reply, verify", "`gh run rerun` refusing with \"already running\" → cancel the run first,"], "en": ["CI never started** → kick it:", "Red caused by this branch** → fix it (the `pr-ready` loop), push,", "Flake / infra red** → rerun the failed jobs; repeat until green. Note", "New review threads** → handle per `pr-ready` (fix or reply, verify", "`gh run rerun` refusing with \"already running\" → cancel the run first,"]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "clean", "no-tropes"], "sourcePath": "skills/babysit/SKILL.md"},
  {"name": "clean", "category": "pr-lifecycle", "invocation": "user", "description": {"id": "Skill autonomous engineering Brooklyn untuk /clean.", "en": "Brooklyn autonomous engineering skill for /clean."}, "detailedDescription": {"id": "# Clean\n\nKISS/DRY and copy the local code style. Be elitist, shorthand, clever, concise,\nefficient, and elegant.\n\n**\"Clean\" means polishing the code — it does NOT mean running the test suite,\ntsc, or lint as the task.** Run a check only if it's genuinely needed to confirm\nthe polish is safe.\n\nClean is a standalone pass, not just a pre-PR step — run it whenever things get\nmessy, including mid-work. Also run it automatically before any PR handoff\n(creating, updating, automerging, or moving to the next PR) without being told\neach time.\n\n## Do\n\n1. Re-read your diff. Cut dead code, leftover debug logging, and duplication.\n2. Extend existing helpers — don't parallel-implement.\n3. Match neighboring style (names, imports, file layout, comment density).\n4. Prefer the small sharp version over ceremony.\n5. UI work: only after they like the UI (`ui-only`). Use `ui-system` for\n   primitives.\n6. On the PR: title/body describe the change as a whole (a short intro), not a\n   changelog of steps. Split into topical commits (`pr-update`), run\n   `no-tropes` on the text, and link the PR.\n\n## Don't\n\n- Run the test suite / tsc / lint just because you were told to \"clean\"\n- Unrelated refactors\n- New abstractions with one caller\n- Scope creep dressed up as cleanup", "en": "# Clean\n\nKISS/DRY and copy the local code style. Be elitist, shorthand, clever, concise,\nefficient, and elegant.\n\n**\"Clean\" means polishing the code — it does NOT mean running the test suite,\ntsc, or lint as the task.** Run a check only if it's genuinely needed to confirm\nthe polish is safe.\n\nClean is a standalone pass, not just a pre-PR step — run it whenever things get\nmessy, including mid-work. Also run it automatically before any PR handoff\n(creating, updating, automerging, or moving to the next PR) without being told\neach time.\n\n## Do\n\n1. Re-read your diff. Cut dead code, leftover debug logging, and duplication.\n2. Extend existing helpers — don't parallel-implement.\n3. Match neighboring style (names, imports, file layout, comment density).\n4. Prefer the small sharp version over ceremony.\n5. UI work: only after they like the UI (`ui-only`). Use `ui-system` for\n   primitives.\n6. On the PR: title/body describe the change as a whole (a short intro), not a\n   changelog of steps. Split into topical commits (`pr-update`), run\n   `no-tropes` on the text, and link the PR.\n\n## Don't\n\n- Run the test suite / tsc / lint just because you were told to \"clean\"\n- Unrelated refactors\n- New abstractions with one caller\n- Scope creep dressed up as cleanup"}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /clean.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /clean.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /clean pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /clean protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["Run the test suite / tsc / lint just because you were told to \"clean\"", "Unrelated refactors", "New abstractions with one caller", "Scope creep dressed up as cleanup"], "en": ["Run the test suite / tsc / lint just because you were told to \"clean\"", "Unrelated refactors", "New abstractions with one caller", "Scope creep dressed up as cleanup"]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "no-tropes", "babysit"], "sourcePath": "skills/clean/SKILL.md"},
  {"name": "cpr", "category": "pr-lifecycle", "invocation": "user", "description": {"id": "Skill autonomous engineering Brooklyn untuk /cpr.", "en": "Brooklyn autonomous engineering skill for /cpr."}, "detailedDescription": {"id": "# CPR (clean → PR)\n\n`clean` then `pr-update`, in that order, without asking between them. Use it\nwhen the work is done and the branch should end up as a PR someone can read.\n\nRun the two skills for real — this file is the sequence, not a replacement for\ntheir contents.\n\n## Steps\n\n1. **Clean** (`clean`). Re-read the diff, cut dead code and debug logging,\n   extend existing helpers instead of paralleling them, match neighboring\n   style. Polish only — this is not a test/tsc/lint run.\n2. **Shape + publish** (`pr-update`). Topical commits, title and body that\n   describe the change as a whole, media in an existing body preserved\n   verbatim, prose through `no-tropes`.\n3. End with the PR/MR as a markdown link — number **and** full forge URL.\n\n## Order matters\n\nClean first, always. Committing then cleaning means the commit split describes\ncode you already threw away, and the PR body describes a diff that no longer\nexists.\n\n## Stop conditions\n\n- UI work: don't start until the user likes the UI (`ui-only`).\n- Nothing to clean is a valid outcome — say so in one line and go to step 2.\n- Don't stall on a clean pass. If polish keeps expanding, that's scope creep;\n  ship the narrow version.\n\n## Scope\n\n- **In:** polish the diff, shape commits, create or update the PR/MR, print the\n  link.\n- **Out:** CI and review threads (`pr-ready`), triage/salvage (`pr-triage`),\n  stacked bases (`stacked-pr`), merging.", "en": "# CPR (clean → PR)\n\n`clean` then `pr-update`, in that order, without asking between them. Use it\nwhen the work is done and the branch should end up as a PR someone can read.\n\nRun the two skills for real — this file is the sequence, not a replacement for\ntheir contents.\n\n## Steps\n\n1. **Clean** (`clean`). Re-read the diff, cut dead code and debug logging,\n   extend existing helpers instead of paralleling them, match neighboring\n   style. Polish only — this is not a test/tsc/lint run.\n2. **Shape + publish** (`pr-update`). Topical commits, title and body that\n   describe the change as a whole, media in an existing body preserved\n   verbatim, prose through `no-tropes`.\n3. End with the PR/MR as a markdown link — number **and** full forge URL.\n\n## Order matters\n\nClean first, always. Committing then cleaning means the commit split describes\ncode you already threw away, and the PR body describes a diff that no longer\nexists.\n\n## Stop conditions\n\n- UI work: don't start until the user likes the UI (`ui-only`).\n- Nothing to clean is a valid outcome — say so in one line and go to step 2.\n- Don't stall on a clean pass. If polish keeps expanding, that's scope creep;\n  ship the narrow version.\n\n## Scope\n\n- **In:** polish the diff, shape commits, create or update the PR/MR, print the\n  link.\n- **Out:** CI and review threads (`pr-ready`), triage/salvage (`pr-triage`),\n  stacked bases (`stacked-pr`), merging."}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /cpr.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /cpr.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /cpr pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /cpr protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["UI work: don't start until the user likes the UI (`ui-only`).", "Nothing to clean is a valid outcome — say so in one line and go to step 2.", "Don't stall on a clean pass. If polish keeps expanding, that's scope creep;", "In:** polish the diff, shape commits, create or update the PR/MR, print the", "Out:** CI and review threads (`pr-ready`), triage/salvage (`pr-triage`),"], "en": ["UI work: don't start until the user likes the UI (`ui-only`).", "Nothing to clean is a valid outcome — say so in one line and go to step 2.", "Don't stall on a clean pass. If polish keeps expanding, that's scope creep;", "In:** polish the diff, shape commits, create or update the PR/MR, print the", "Out:** CI and review threads (`pr-ready`), triage/salvage (`pr-triage`),"]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["clean", "no-tropes", "babysit"], "sourcePath": "skills/cpr/SKILL.md"},
  {"name": "draft-tweet", "category": "system", "invocation": "model", "description": {"id": "Skill autonomous engineering Brooklyn untuk /draft-tweet.", "en": "Brooklyn autonomous engineering skill for /draft-tweet."}, "detailedDescription": {"id": "# Draft Tweet\n\nWrite tweet options. **Do not post** unless they explicitly say to.\n\n## Match their voice\n\nIf `xurl` is installed and already authenticated, use it read-only to sample\nthe user's recent original posts before drafting:\n\n```bash\nxurl whoami\nxurl posts @handle -n 30\n```\n\nIf that version does not have `xurl posts`, use the user ID from `xurl whoami`:\n\n```bash\nxurl '/2/users/<id>/tweets?max_results=30&exclude=retweets,replies'\n```\n\nInfer their usual capitalization, punctuation, sentence length, vocabulary,\nlink use, and emoji use. Match the patterns, not the subject matter. Do not\nquote old posts or mention that style sampling happened.\n\nIf `xurl` is missing or unauthenticated, draft from examples already in the\nconversation. Do not block the task or ask them to configure X unless they\nspecifically request stronger style matching.\n\n## Output\n\n2–4 options, each roughly ≤240 chars:\n\n- Concrete claim or user benefit — no empty hype\n- Little/no emoji; no engagement bait\n- One link max if useful\n- Run through `no-tropes`\n\n## Don't\n\n- Call `xurl post`, reply, quote, DM, or any other write API unless asked\n- Invent performance numbers they didn't bless\n- Default to a thread — offer one only if they want depth", "en": "# Draft Tweet\n\nWrite tweet options. **Do not post** unless they explicitly say to.\n\n## Match their voice\n\nIf `xurl` is installed and already authenticated, use it read-only to sample\nthe user's recent original posts before drafting:\n\n```bash\nxurl whoami\nxurl posts @handle -n 30\n```\n\nIf that version does not have `xurl posts`, use the user ID from `xurl whoami`:\n\n```bash\nxurl '/2/users/<id>/tweets?max_results=30&exclude=retweets,replies'\n```\n\nInfer their usual capitalization, punctuation, sentence length, vocabulary,\nlink use, and emoji use. Match the patterns, not the subject matter. Do not\nquote old posts or mention that style sampling happened.\n\nIf `xurl` is missing or unauthenticated, draft from examples already in the\nconversation. Do not block the task or ask them to configure X unless they\nspecifically request stronger style matching.\n\n## Output\n\n2–4 options, each roughly ≤240 chars:\n\n- Concrete claim or user benefit — no empty hype\n- Little/no emoji; no engagement bait\n- One link max if useful\n- Run through `no-tropes`\n\n## Don't\n\n- Call `xurl post`, reply, quote, DM, or any other write API unless asked\n- Invent performance numbers they didn't bless\n- Default to a thread — offer one only if they want depth"}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /draft-tweet.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /draft-tweet.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /draft-tweet pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /draft-tweet protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["Concrete claim or user benefit — no empty hype", "Little/no emoji; no engagement bait", "One link max if useful", "Run through `no-tropes`", "Call `xurl post`, reply, quote, DM, or any other write API unless asked"], "en": ["Concrete claim or user benefit — no empty hype", "Little/no emoji; no engagement bait", "One link max if useful", "Run through `no-tropes`", "Call `xurl post`, reply, quote, DM, or any other write API unless asked"]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "clean", "no-tropes"], "sourcePath": "skills/draft-tweet/SKILL.md"},
  {"name": "free-disk-space", "category": "system", "invocation": "model", "description": {"id": "Skill autonomous engineering Brooklyn untuk /free-disk-space.", "en": "Brooklyn autonomous engineering skill for /free-disk-space."}, "detailedDescription": {"id": "# Free Disk Space\n\nInvestigate first. Delete only regenerable data the user has authorized.\n\n## Safety\n\n- Never inspect, delete, or modify the data of the IDE or agent you're running\n  inside — its application-support dir, caches, `globalStorage`, databases,\n  extensions, or logs — unless the user explicitly names it as a cleanup target.\n  On macOS that lives under `~/Library/Application Support/<app>/` (e.g.\n  `.../Cursor/`).\n- Never delete browser history, extensions, cookies, credentials, local\n  storage, IndexedDB, bookmarks, passwords, or user-created files.\n- Ask before destructive cleanup such as deleting Docker images, containers,\n  volumes, or virtual disks.\n\n## Workflow\n\n1. Record current usage from the macOS data volume:\n\n   ```bash\n   df -h /System/Volumes/Data\n   ```\n\n2. Find the largest consumers before deleting anything. Use `du`, package\n   manager diagnostics, and application-specific storage commands. Do not\n   descend into protected agent/IDE data.\n3. Explain what is large, whether it is regenerable, and the expected impact.\n4. Clean only safe targets:\n   - contents of `~/Library/Caches/`\n   - unused package-manager cache entries, such as `pnpm store prune`\n   - known browser model/GPU caches, never browser profile data\n5. For sparse files such as Docker's virtual disk, report actual disk use from\n   `du`, not apparent size from `ls`.\n6. Record usage again and report space freed.\n\nDo not represent the sealed `/` system volume as the Mac's usable data-volume\ncapacity.", "en": "# Free Disk Space\n\nInvestigate first. Delete only regenerable data the user has authorized.\n\n## Safety\n\n- Never inspect, delete, or modify the data of the IDE or agent you're running\n  inside — its application-support dir, caches, `globalStorage`, databases,\n  extensions, or logs — unless the user explicitly names it as a cleanup target.\n  On macOS that lives under `~/Library/Application Support/<app>/` (e.g.\n  `.../Cursor/`).\n- Never delete browser history, extensions, cookies, credentials, local\n  storage, IndexedDB, bookmarks, passwords, or user-created files.\n- Ask before destructive cleanup such as deleting Docker images, containers,\n  volumes, or virtual disks.\n\n## Workflow\n\n1. Record current usage from the macOS data volume:\n\n   ```bash\n   df -h /System/Volumes/Data\n   ```\n\n2. Find the largest consumers before deleting anything. Use `du`, package\n   manager diagnostics, and application-specific storage commands. Do not\n   descend into protected agent/IDE data.\n3. Explain what is large, whether it is regenerable, and the expected impact.\n4. Clean only safe targets:\n   - contents of `~/Library/Caches/`\n   - unused package-manager cache entries, such as `pnpm store prune`\n   - known browser model/GPU caches, never browser profile data\n5. For sparse files such as Docker's virtual disk, report actual disk use from\n   `du`, not apparent size from `ls`.\n6. Record usage again and report space freed.\n\nDo not represent the sealed `/` system volume as the Mac's usable data-volume\ncapacity."}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /free-disk-space.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /free-disk-space.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /free-disk-space pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /free-disk-space protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["Never inspect, delete, or modify the data of the IDE or agent you're running", "Never delete browser history, extensions, cookies, credentials, local", "Ask before destructive cleanup such as deleting Docker images, containers,", "contents of `~/Library/Caches/`", "unused package-manager cache entries, such as `pnpm store prune`"], "en": ["Never inspect, delete, or modify the data of the IDE or agent you're running", "Never delete browser history, extensions, cookies, credentials, local", "Ask before destructive cleanup such as deleting Docker images, containers,", "contents of `~/Library/Caches/`", "unused package-manager cache entries, such as `pnpm store prune`"]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "clean", "no-tropes"], "sourcePath": "skills/free-disk-space/SKILL.md"},
  {"name": "list-open-work", "category": "pr-lifecycle", "invocation": "model", "description": {"id": "Skill autonomous engineering Brooklyn untuk /list-open-work.", "en": "Brooklyn autonomous engineering skill for /list-open-work."}, "detailedDescription": {"id": "# /list-open-work\n\nAnswer one question: what of mine is open **in this repo**, and which ticket\ndoes each carry? Lead with the list. Anything else goes after it, in prose.\n\n## This repo only\n\nScope is the git checkout you are in (worktree or primary). Read its `origin`.\nList **my** open MRs/PRs against **that** remote. Stop there.\n\n- Not a git repo → say so and stop. Do not hunt another project.\n- Do not list another origin, org, forge, or tracker from memory, another\n  session, or another worktree.\n- Infer the ticket key from **this** repo's branches/titles (e.g. `PROJ-123`\n  in `feat/PROJ-123-slug`). Never reuse a prefix you saw somewhere else.\n\n```bash\ngit rev-parse --show-toplevel\ngit remote get-url origin\n```\n\n## Output format\n\nWrite it straight into the chat as ordinary rendered markdown. **Never wrap it\nin a code fence** — the links have to be clickable.\n\n**Blank line between every item.** Markdown treats a single newline as a space,\nso adjacent lines become one paragraph. That is a failure. Each MR is its own\nparagraph: newline, blank line, next line.\n\nExactly this shape. Header is `Open MRs:` or `Open PRs:`. No bullets, no emoji.\nThe number and the ticket key are both links. Ticket sits in trailing `[KEY]`.\n\nOpen MRs:\n\n[!42](https://gitlab.example.com/acme/widgets/-/merge_requests/42) - shorten the save path [[PROJ-101](https://jira.example.com/browse/PROJ-101)]\n\n[!41](https://gitlab.example.com/acme/widgets/-/merge_requests/41) - default the new flag on [[PROJ-99](https://jira.example.com/browse/PROJ-99)]\n\n[!30](https://gitlab.example.com/acme/widgets/-/merge_requests/30) - expose status history on the public API\n\n- Descending by number. `!N` on GitLab, `#N` on GitHub. \"MRs\" vs \"PRs\" likewise.\n- One paragraph each: `<!|#><N> - <title> [<KEY>]`. No bold.\n- Ticket in trailing `[KEY]`. Move it there if the author buried it mid-title:\n  `perf: PROJ-77 — cut over-fetch` becomes `perf: cut over-fetch [PROJ-77]`.\n- No ticket → omit the `[KEY]`. Don't invent one, don't write `(no ticket)`.\n- Keep the MR/PR title as-is (minus the relocated key). Don't rewrite it.\n\nWrong (renders as one wrapped line):\n\n```\nOpen MRs:\n!42 - shorten the save path [PROJ-101]\n!41 - default the new flag on [PROJ-99]\n```\n\nPasting into Slack is a separate ask. Forge/tracker integrations linkify `!42`\nand `PROJ-101` on their own, so that version is the same lines with the link\nsyntax stripped — still one item per line.\n\n## Collect\n\nMine means authored by me. Resolve my username from the API — never guess it.\n\nGitLab origin:\n\n```bash\nglab api user\nglab api \"projects/<url-encoded-path>/merge_requests?state=opened&scope=all&per_page=100\"\n```\n\nFilter to author = me. `<url-encoded-path>` is **this** origin's path, not a\npath from another repo.\n\nGitHub origin: `gh pr list --author @me --state open --json number,title,headRefName,body`\n\nTicket = first `\\b[A-Z][A-Z0-9]+-\\d+\\b` in the title, then the branch, then the\ndescription. Use that key's tracker host as this repo uses it — don't guess a\nsite from another company.\n\n## Then, in prose\n\nOnly the things that change what I'd do next:\n\n- Conflicts with the default branch — blocked, not in review. GitLab recomputes\n  mergeability lazily, so a plain MR GET happily returns a stale\n  `has_conflicts: false`. Force it and read `merge_status`:\n\n```bash\nglab api \"projects/<path>/merge_requests/<iid>?include_diverged_commits_count=true\"\n# cannot_be_merged + has_conflicts → needs a rebase; diverged_commits_count = how far behind\n```\n\n- Red pipelines (`head_pipeline.status`).\n- Ticket status that contradicts the MR: green with every thread resolved but the\n  ticket still says In Progress.\n- No ticket plus months of age — give the age, offer to close.\n\nSkip approvals unless asked. Don't narrate fields that changed nothing.", "en": "# /list-open-work\n\nAnswer one question: what of mine is open **in this repo**, and which ticket\ndoes each carry? Lead with the list. Anything else goes after it, in prose.\n\n## This repo only\n\nScope is the git checkout you are in (worktree or primary). Read its `origin`.\nList **my** open MRs/PRs against **that** remote. Stop there.\n\n- Not a git repo → say so and stop. Do not hunt another project.\n- Do not list another origin, org, forge, or tracker from memory, another\n  session, or another worktree.\n- Infer the ticket key from **this** repo's branches/titles (e.g. `PROJ-123`\n  in `feat/PROJ-123-slug`). Never reuse a prefix you saw somewhere else.\n\n```bash\ngit rev-parse --show-toplevel\ngit remote get-url origin\n```\n\n## Output format\n\nWrite it straight into the chat as ordinary rendered markdown. **Never wrap it\nin a code fence** — the links have to be clickable.\n\n**Blank line between every item.** Markdown treats a single newline as a space,\nso adjacent lines become one paragraph. That is a failure. Each MR is its own\nparagraph: newline, blank line, next line.\n\nExactly this shape. Header is `Open MRs:` or `Open PRs:`. No bullets, no emoji.\nThe number and the ticket key are both links. Ticket sits in trailing `[KEY]`.\n\nOpen MRs:\n\n[!42](https://gitlab.example.com/acme/widgets/-/merge_requests/42) - shorten the save path [[PROJ-101](https://jira.example.com/browse/PROJ-101)]\n\n[!41](https://gitlab.example.com/acme/widgets/-/merge_requests/41) - default the new flag on [[PROJ-99](https://jira.example.com/browse/PROJ-99)]\n\n[!30](https://gitlab.example.com/acme/widgets/-/merge_requests/30) - expose status history on the public API\n\n- Descending by number. `!N` on GitLab, `#N` on GitHub. \"MRs\" vs \"PRs\" likewise.\n- One paragraph each: `<!|#><N> - <title> [<KEY>]`. No bold.\n- Ticket in trailing `[KEY]`. Move it there if the author buried it mid-title:\n  `perf: PROJ-77 — cut over-fetch` becomes `perf: cut over-fetch [PROJ-77]`.\n- No ticket → omit the `[KEY]`. Don't invent one, don't write `(no ticket)`.\n- Keep the MR/PR title as-is (minus the relocated key). Don't rewrite it.\n\nWrong (renders as one wrapped line):\n\n```\nOpen MRs:\n!42 - shorten the save path [PROJ-101]\n!41 - default the new flag on [PROJ-99]\n```\n\nPasting into Slack is a separate ask. Forge/tracker integrations linkify `!42`\nand `PROJ-101` on their own, so that version is the same lines with the link\nsyntax stripped — still one item per line.\n\n## Collect\n\nMine means authored by me. Resolve my username from the API — never guess it.\n\nGitLab origin:\n\n```bash\nglab api user\nglab api \"projects/<url-encoded-path>/merge_requests?state=opened&scope=all&per_page=100\"\n```\n\nFilter to author = me. `<url-encoded-path>` is **this** origin's path, not a\npath from another repo.\n\nGitHub origin: `gh pr list --author @me --state open --json number,title,headRefName,body`\n\nTicket = first `\\b[A-Z][A-Z0-9]+-\\d+\\b` in the title, then the branch, then the\ndescription. Use that key's tracker host as this repo uses it — don't guess a\nsite from another company.\n\n## Then, in prose\n\nOnly the things that change what I'd do next:\n\n- Conflicts with the default branch — blocked, not in review. GitLab recomputes\n  mergeability lazily, so a plain MR GET happily returns a stale\n  `has_conflicts: false`. Force it and read `merge_status`:\n\n```bash\nglab api \"projects/<path>/merge_requests/<iid>?include_diverged_commits_count=true\"\n# cannot_be_merged + has_conflicts → needs a rebase; diverged_commits_count = how far behind\n```\n\n- Red pipelines (`head_pipeline.status`).\n- Ticket status that contradicts the MR: green with every thread resolved but the\n  ticket still says In Progress.\n- No ticket plus months of age — give the age, offer to close.\n\nSkip approvals unless asked. Don't narrate fields that changed nothing."}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /list-open-work.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /list-open-work.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /list-open-work pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /list-open-work protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["Not a git repo → say so and stop. Do not hunt another project.", "Do not list another origin, org, forge, or tracker from memory, another", "Infer the ticket key from **this** repo's branches/titles (e.g. `PROJ-123`", "Descending by number. `!N` on GitLab, `#N` on GitHub. \"MRs\" vs \"PRs\" likewise.", "One paragraph each: `<!|#><N> - <title> [<KEY>]`. No bold."], "en": ["Not a git repo → say so and stop. Do not hunt another project.", "Do not list another origin, org, forge, or tracker from memory, another", "Infer the ticket key from **this** repo's branches/titles (e.g. `PROJ-123`", "Descending by number. `!N` on GitLab, `#N` on GitHub. \"MRs\" vs \"PRs\" likewise.", "One paragraph each: `<!|#><N> - <title> [<KEY>]`. No bold."]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "clean", "no-tropes"], "sourcePath": "skills/list-open-work/SKILL.md"},
  {"name": "no-tropes", "category": "quality", "invocation": "user", "description": {"id": "Skill autonomous engineering Brooklyn untuk /no-tropes.", "en": "Brooklyn autonomous engineering skill for /no-tropes."}, "detailedDescription": {"id": "# No Tropes (AI writing tells)\n\nAfter drafting any prose, revise it against the tropes catalog in [tropes-reference.md](tropes-reference.md).\n\n## Core Principle\n\nWrite like a human: varied, imperfect, specific. Any single trope used once might be fine. The problem is when multiple tropes stack or a single trope repeats.\n\n## Quick Checklist\n\nBefore finalizing text, scan for:\n\n- **Word choice**: \"quietly\", \"delve\", \"tapestry\", \"landscape\", \"serves as\", \"leverage\", \"robust\"\n- **Sentence structure**: \"It's not X — it's Y\", \"The X? A Y.\", tricolons, anaphora, false ranges, \"-ing\" tacked-on analysis\n- **Paragraph structure**: Short punchy fragments as standalone paragraphs, listicles disguised as prose\n- **Tone**: \"Here's the thing\", \"Think of it as\", \"Imagine a world\", \"Let's break this down\", invented concept labels, grandiose stakes inflation\n- **Formatting**: Em-dash overuse (2-3 max per piece), bold-first bullets, unicode arrows (→)\n- **Composition**: Fractal summaries, dead metaphors beaten into the ground, historical analogy stacking, one-point dilution, \"In conclusion\"\n\n## Revision Protocol\n\n1. Read the full draft\n2. Flag any trope matches from the reference\n3. Rewrite flagged passages using plain, direct language\n4. Prefer simple verbs (\"is\" over \"serves as\"), concrete nouns, and varied sentence structure\n5. Cut filler transitions (\"It's worth noting\", \"Importantly\", \"Interestingly\")\n6. If you catch yourself writing the same structural pattern 3+ times, break the pattern", "en": "# No Tropes (AI writing tells)\n\nAfter drafting any prose, revise it against the tropes catalog in [tropes-reference.md](tropes-reference.md).\n\n## Core Principle\n\nWrite like a human: varied, imperfect, specific. Any single trope used once might be fine. The problem is when multiple tropes stack or a single trope repeats.\n\n## Quick Checklist\n\nBefore finalizing text, scan for:\n\n- **Word choice**: \"quietly\", \"delve\", \"tapestry\", \"landscape\", \"serves as\", \"leverage\", \"robust\"\n- **Sentence structure**: \"It's not X — it's Y\", \"The X? A Y.\", tricolons, anaphora, false ranges, \"-ing\" tacked-on analysis\n- **Paragraph structure**: Short punchy fragments as standalone paragraphs, listicles disguised as prose\n- **Tone**: \"Here's the thing\", \"Think of it as\", \"Imagine a world\", \"Let's break this down\", invented concept labels, grandiose stakes inflation\n- **Formatting**: Em-dash overuse (2-3 max per piece), bold-first bullets, unicode arrows (→)\n- **Composition**: Fractal summaries, dead metaphors beaten into the ground, historical analogy stacking, one-point dilution, \"In conclusion\"\n\n## Revision Protocol\n\n1. Read the full draft\n2. Flag any trope matches from the reference\n3. Rewrite flagged passages using plain, direct language\n4. Prefer simple verbs (\"is\" over \"serves as\"), concrete nouns, and varied sentence structure\n5. Cut filler transitions (\"It's worth noting\", \"Importantly\", \"Interestingly\")\n6. If you catch yourself writing the same structural pattern 3+ times, break the pattern"}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /no-tropes.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /no-tropes.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /no-tropes pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /no-tropes protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["Word choice**: \"quietly\", \"delve\", \"tapestry\", \"landscape\", \"serves as\", \"leverage\", \"robust\"", "Sentence structure**: \"It's not X — it's Y\", \"The X? A Y.\", tricolons, anaphora, false ranges, \"-ing\" tacked-on analysis", "Paragraph structure**: Short punchy fragments as standalone paragraphs, listicles disguised as prose", "Tone**: \"Here's the thing\", \"Think of it as\", \"Imagine a world\", \"Let's break this down\", invented concept labels, grandiose stakes inflation", "Formatting**: Em-dash overuse (2-3 max per piece), bold-first bullets, unicode arrows (→)"], "en": ["Word choice**: \"quietly\", \"delve\", \"tapestry\", \"landscape\", \"serves as\", \"leverage\", \"robust\"", "Sentence structure**: \"It's not X — it's Y\", \"The X? A Y.\", tricolons, anaphora, false ranges, \"-ing\" tacked-on analysis", "Paragraph structure**: Short punchy fragments as standalone paragraphs, listicles disguised as prose", "Tone**: \"Here's the thing\", \"Think of it as\", \"Imagine a world\", \"Let's break this down\", invented concept labels, grandiose stakes inflation", "Formatting**: Em-dash overuse (2-3 max per piece), bold-first bullets, unicode arrows (→)"]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "clean", "babysit"], "sourcePath": "skills/no-tropes/SKILL.md"},
  {"name": "notarize-mac", "category": "system", "invocation": "model", "description": {"id": "Skill autonomous engineering Brooklyn untuk /notarize-mac.", "en": "Brooklyn autonomous engineering skill for /notarize-mac."}, "detailedDescription": {"id": "# Notarize macOS Build\n\nUse the project's existing notarization automation. Don't invent a new signing\npipeline or expose credentials.\n\n## Procedure\n\n1. Confirm the checkout and branch. Default to a clean, current default branch.\n2. Locate the existing notarization/release script (e.g. a\n   `notarize-*-installer.sh` or the release script the project already ships):\n   - use a path the user supplies first;\n   - otherwise search the repo and nearby release/notary checkouts;\n   - if none exists, ask for its location instead of recreating it.\n3. Run the script from the environment it expects.\n4. Preserve its signing identity, App Store Connect key handling, artifact\n   naming, and notarization flow.\n5. Verify the resulting artifact with the script's checks or `spctl`.\n6. Report the artifact path, size, notarization result, and Gatekeeper result.\n\nNever print private key contents, signing credentials, or secret values.", "en": "# Notarize macOS Build\n\nUse the project's existing notarization automation. Don't invent a new signing\npipeline or expose credentials.\n\n## Procedure\n\n1. Confirm the checkout and branch. Default to a clean, current default branch.\n2. Locate the existing notarization/release script (e.g. a\n   `notarize-*-installer.sh` or the release script the project already ships):\n   - use a path the user supplies first;\n   - otherwise search the repo and nearby release/notary checkouts;\n   - if none exists, ask for its location instead of recreating it.\n3. Run the script from the environment it expects.\n4. Preserve its signing identity, App Store Connect key handling, artifact\n   naming, and notarization flow.\n5. Verify the resulting artifact with the script's checks or `spctl`.\n6. Report the artifact path, size, notarization result, and Gatekeeper result.\n\nNever print private key contents, signing credentials, or secret values."}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /notarize-mac.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /notarize-mac.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /notarize-mac pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /notarize-mac protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["use a path the user supplies first;", "otherwise search the repo and nearby release/notary checkouts;", "if none exists, ask for its location instead of recreating it."], "en": ["use a path the user supplies first;", "otherwise search the repo and nearby release/notary checkouts;", "if none exists, ask for its location instead of recreating it."]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "clean", "no-tropes"], "sourcePath": "skills/notarize-mac/SKILL.md"},
  {"name": "perf", "category": "quality", "invocation": "model", "description": {"id": "Skill autonomous engineering Brooklyn untuk /perf.", "en": "Brooklyn autonomous engineering skill for /perf."}, "detailedDescription": {"id": "# Perf\n\nThe general performance loop. Profiling (CPU profile, flamegraph, memory\nsnapshot, trace) is the usual tool, but the loop is the same regardless of which\none fits.\n\n## Loop\n\n1. Reproduce the slow path and capture a baseline number — time, memory, FPS,\n   request latency, whatever matters here.\n2. Profile to find the real hot path: CPU profile / flamegraph / memory snapshot\n   / trace as fits. Reuse the repo's existing profiler; don't build a parallel\n   harness.\n3. Read the profile, not your intuition — fix the actual hot path.\n4. Re-measure. Record before/after in the PR title/description.\n5. Ship in topical commits (`pr-update`), `clean`, keep CI green.\n\n## Don't\n\n- Optimize by guess before profiling.\n- Fiddle with the measurement system instead of improving perf.\n- Run a full suite or profile that overwhelms the machine — scope the run.\n- Report gains you didn't measure.", "en": "# Perf\n\nThe general performance loop. Profiling (CPU profile, flamegraph, memory\nsnapshot, trace) is the usual tool, but the loop is the same regardless of which\none fits.\n\n## Loop\n\n1. Reproduce the slow path and capture a baseline number — time, memory, FPS,\n   request latency, whatever matters here.\n2. Profile to find the real hot path: CPU profile / flamegraph / memory snapshot\n   / trace as fits. Reuse the repo's existing profiler; don't build a parallel\n   harness.\n3. Read the profile, not your intuition — fix the actual hot path.\n4. Re-measure. Record before/after in the PR title/description.\n5. Ship in topical commits (`pr-update`), `clean`, keep CI green.\n\n## Don't\n\n- Optimize by guess before profiling.\n- Fiddle with the measurement system instead of improving perf.\n- Run a full suite or profile that overwhelms the machine — scope the run.\n- Report gains you didn't measure."}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /perf.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /perf.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /perf pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /perf protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["Optimize by guess before profiling.", "Fiddle with the measurement system instead of improving perf.", "Run a full suite or profile that overwhelms the machine — scope the run.", "Report gains you didn't measure."], "en": ["Optimize by guess before profiling.", "Fiddle with the measurement system instead of improving perf.", "Run a full suite or profile that overwhelms the machine — scope the run.", "Report gains you didn't measure."]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "clean", "no-tropes"], "sourcePath": "skills/perf/SKILL.md"},
  {"name": "pr-ready", "category": "pr-lifecycle", "invocation": "model", "description": {"id": "Skill autonomous engineering Brooklyn untuk /pr-ready.", "en": "Brooklyn autonomous engineering skill for /pr-ready."}, "detailedDescription": {"id": "# PR Ready\n\nAn open PR/MR isn't mergeable. Two things block it — a **stale base or red CI**,\nand **open review threads**. Same loop for both: fix, push, re-query the forge,\nrepeat. Handle whichever is blocking; usually that's both.\n\nNo feature work. No drive-bys.\n\n## Rebase + CI\n\n1. Find the worktree for this PR (or create one per repo convention). Prefer an\n   existing worktree the user already has.\n2. `git fetch origin <default>` (usually `main`).\n3. Rebase onto `origin/<default>`. Fix conflicts only as needed to preserve intent.\n4. Push (`--force-with-lease` if the rebase rewrote history).\n5. Watch CI. Fix **failures caused by the rebase or existing breakage on this\n   branch** — nothing else.\n6. Flaky upstream on the default branch → say so. Don't paper over it with\n   unrelated test deletes.\n\n## Review threads\n\n1. Commit/push any unpushed work first (ask if the message is unclear).\n2. List open/unresolved inline comments + review bodies.\n3. Want a bot pass? Request the repo's review bot, then wait for it — don't spin\n   forever; if it's stuck ~10–15m, say so.\n4. Per comment: valid → fix and resolve. Wrong or outdated → short reply and\n   resolve. Nit out of scope → ask once. Never resolve a #1 maintainer comment\n   without fixing it or an explicit override (`pr-triage`).\n5. Push (`pr-update` if they want the commits split).\n6. Repeat until unresolved threads are gone, or only ones they OK'd deferring.\n\n## Done when\n\nRe-query the forge — don't trust that a push turned CI green or that a reply\nresolved a thread. **Fail closed:** any red check or open thread means you're\nnot done.\n\nReport the branch tip, checks status, and a short fixed-vs-replied summary.\n\n## Scope lock\n\n- No refactors, restyles, or \"while I'm here\" fixes.\n- Don't expand the PR description or supersede unless asked (`pr-update`).\n- Wrong-premise PR that needs salvage → stop and say so, hand off to\n  `pr-triage`. Don't silently rewrite it.\n\n## Forge cheatsheet\n\n```bash\n# GitHub — CI\ngh pr view <N> --json url,headRefName,baseRefName,statusCheckRollup\ngh pr checks <N>\n\n# GitHub — threads\ngh api repos/{owner}/{repo}/pulls/<N>/comments --jq '.[] | {user:.user.login,path,line,body:(.body[:200])}'\ngh api repos/{owner}/{repo}/pulls/<N>/reviews  --jq '.[] | {user:.user.login,state,body:(.body[:200])}'\ngh api repos/{owner}/{repo}/pulls/<N>/comments -f body='…' -F in_reply_to=<comment_id>\ngh pr edit <N> --add-reviewer copilot   # if that's what the repo uses\n\n# GitLab\nglab mr view <N>\nglab ci status\nglab mr note <N>        # + the discussions API for resolve\n```", "en": "# PR Ready\n\nAn open PR/MR isn't mergeable. Two things block it — a **stale base or red CI**,\nand **open review threads**. Same loop for both: fix, push, re-query the forge,\nrepeat. Handle whichever is blocking; usually that's both.\n\nNo feature work. No drive-bys.\n\n## Rebase + CI\n\n1. Find the worktree for this PR (or create one per repo convention). Prefer an\n   existing worktree the user already has.\n2. `git fetch origin <default>` (usually `main`).\n3. Rebase onto `origin/<default>`. Fix conflicts only as needed to preserve intent.\n4. Push (`--force-with-lease` if the rebase rewrote history).\n5. Watch CI. Fix **failures caused by the rebase or existing breakage on this\n   branch** — nothing else.\n6. Flaky upstream on the default branch → say so. Don't paper over it with\n   unrelated test deletes.\n\n## Review threads\n\n1. Commit/push any unpushed work first (ask if the message is unclear).\n2. List open/unresolved inline comments + review bodies.\n3. Want a bot pass? Request the repo's review bot, then wait for it — don't spin\n   forever; if it's stuck ~10–15m, say so.\n4. Per comment: valid → fix and resolve. Wrong or outdated → short reply and\n   resolve. Nit out of scope → ask once. Never resolve a #1 maintainer comment\n   without fixing it or an explicit override (`pr-triage`).\n5. Push (`pr-update` if they want the commits split).\n6. Repeat until unresolved threads are gone, or only ones they OK'd deferring.\n\n## Done when\n\nRe-query the forge — don't trust that a push turned CI green or that a reply\nresolved a thread. **Fail closed:** any red check or open thread means you're\nnot done.\n\nReport the branch tip, checks status, and a short fixed-vs-replied summary.\n\n## Scope lock\n\n- No refactors, restyles, or \"while I'm here\" fixes.\n- Don't expand the PR description or supersede unless asked (`pr-update`).\n- Wrong-premise PR that needs salvage → stop and say so, hand off to\n  `pr-triage`. Don't silently rewrite it.\n\n## Forge cheatsheet\n\n```bash\n# GitHub — CI\ngh pr view <N> --json url,headRefName,baseRefName,statusCheckRollup\ngh pr checks <N>\n\n# GitHub — threads\ngh api repos/{owner}/{repo}/pulls/<N>/comments --jq '.[] | {user:.user.login,path,line,body:(.body[:200])}'\ngh api repos/{owner}/{repo}/pulls/<N>/reviews  --jq '.[] | {user:.user.login,state,body:(.body[:200])}'\ngh api repos/{owner}/{repo}/pulls/<N>/comments -f body='…' -F in_reply_to=<comment_id>\ngh pr edit <N> --add-reviewer copilot   # if that's what the repo uses\n\n# GitLab\nglab mr view <N>\nglab ci status\nglab mr note <N>        # + the discussions API for resolve\n```"}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /pr-ready.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /pr-ready.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /pr-ready pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /pr-ready protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["No refactors, restyles, or \"while I'm here\" fixes.", "Don't expand the PR description or supersede unless asked (`pr-update`).", "Wrong-premise PR that needs salvage → stop and say so, hand off to"], "en": ["No refactors, restyles, or \"while I'm here\" fixes.", "Don't expand the PR description or supersede unless asked (`pr-update`).", "Wrong-premise PR that needs salvage → stop and say so, hand off to"]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "clean", "no-tropes"], "sourcePath": "skills/pr-ready/SKILL.md"},
  {"name": "pr-triage", "category": "pr-lifecycle", "invocation": "model", "description": {"id": "Skill autonomous engineering Brooklyn untuk /pr-triage.", "en": "Brooklyn autonomous engineering skill for /pr-triage."}, "detailedDescription": {"id": "# PR Triage\n\nMaintainer triage. Three real outcomes: **approve**, **supersede** (salvage +\ncredit + close the old one), or **close** (wrong premise). Verdict first; wait\nfor an explicit **go** before forge writes (approve / supersede / close / merge /\npush). Use `gh` or `glab`.\n\n## Kickoff\n\nBatch (\"we will be reviewing…\") or a pasted Discord/`>>>…<<<` thread with a PR:\n\n1. Figure out repo + forge. Skim `AGENTS.md` / `CONTRIBUTING.md` if present.\n2. Match this repo's worktree/branch naming by inspecting existing worktrees.\n3. Don't touch the primary checkout — use worktrees.\n4. **Cluster the batch first** (below), then verdict + salvage plan; wait for go\n   before forge writes.\n\n## Cluster the batch first (before per-PR verdicts)\n\nMore than one PR/issue in front of you? Group them by the **fix they need**\nbefore you judge any single one. Multiple PRs on the same bug (or fragile\nsubsystem) are **one consolidation**, not N salvages.\n\n1. Map each PR/issue to the underlying fix and the files/subsystem it touches.\n2. Any two that fix the **same bug class or the same subsystem** → treat the\n   group as **one super-PR**: build the proper fix once, credit **every**\n   author (`Co-authored-by` + `@handle`), and supersede the whole cluster.\n3. Default to consolidation. Only keep PRs separate when the fixes are genuinely\n   independent (different bug class, different area, no shared code).\n4. State the grouping up front, e.g. `#68665 + #63590 + #67603 → one super-PR`,\n   then run the per-PR loop **within** each group to confirm premises.\n\nDo **not** enumerate one verdict per PR when they share a fix — that's the lazy\nshape. One cluster → one consolidated supersede.\n\n## Discord threads\n\nA pasted thread *is* the intake. Don't make them restate a bare URL.\n\n1. Pull out symptom, platform, linked PR/issue, what staff already said.\n2. Open those PRs; look for siblings on the same fix.\n3. Run the per-PR loop. Check the PR actually matches the reported bug.\n4. Draft a user reply only if useful — still wait for go before GH actions.\n5. No PR? Search open ones; if they want a new fix, follow that repository's\n   worktree workflow.\n\n## Forge cheatsheet\n\n| Action | GitHub | GitLab |\n|--------|--------|--------|\n| View | `gh pr view <N>` | `glab mr view <N>` |\n| Diff | `gh pr diff <N>` | `glab mr diff <N>` |\n| Comment | `gh pr comment <N>` | `glab mr note <N>` |\n| Review | `gh pr review <N> --approve \\| --request-changes` | `glab mr approve` / note |\n| Close PR/MR | `gh pr close <N>` | `glab mr close <N>` |\n| Close issue | `gh issue close <N>` | `glab issue close <N>` |\n\n```bash\ngit fetch origin pull/<N>/head:refs/remotes/origin/pr-<N>   # GitHub\n# GitLab: glab mr checkout <N>\n```\n\n## Per-PR loop\n\nRun this **within each cluster** (see \"Cluster the batch first\"). If the group\nconsolidates, the salvage plan is shared and you supersede all of them into one\nsuper-PR — don't emit a standalone salvage per member.\n\n1. Metadata + diff + existing reviews (`gh` / `glab`).\n2. Unaddressed **#1 / lead maintainer** comments → do not approve.\n3. Check the premise on current default branch (usually `main`).\n4. Cherry-pick/apply in a review worktree; run targeted tests this repo uses.\n5. Return a verdict:\n\n```markdown\n**PR/MR:** [#N](https://…/pull/N)\n\n**Verdict: approve | supersede | close-as-wrong-premise**\n\n**Worktree:** `…` · `<branch>`\n\n### Why\n- …\n\n### Checks\n| Check | Result |\n|---|---|\n| Merges onto default branch | … |\n| CI | … |\n| Targeted tests | … |\n\n### Next (after go)\n- approve: …\n- supersede: salvage plan (reuse X, drop Y, tests Z, credit @handle)\n- close: wrong-premise comment outline\n```\n\n## Pick the verdict (no soft exits)\n\n| Situation | Verdict |\n|-----------|---------|\n| Idea + shape are fine | **approve** |\n| Idea is right, shape is wrong (wrong helper/layer, missing tests for the bug class, contaminated, stale/conflicting, duplicate of another PR, ignored lead feedback) | **supersede** |\n| Premise doesn't hold on current default branch | **close-as-wrong-premise** |\n\n**Anti-patterns — do not do these:**\n\n- `keep open — ask the author to rewrite / reuse helper X / add coverage`\n  when the shape is already wrong → that **is** supersede. You salvage it.\n- Inventing a fourth outcome (`request-changes`, `needs-info`, `wait-and-see`)\n  for external PRs that need reshaping.\n- Emitting one verdict per PR when several fix the same bug/subsystem → that's\n  **one** consolidated super-PR that supersedes the cluster, credit all authors.\n- Parking on someone else's confirmation when the PR's *shape* is already a\n  supersede (wrong helper, wrong layer, no bug-class tests). Note the open\n  question under Why if useful; still verdict **supersede** with the salvage\n  plan. Only block the verdict when you literally cannot tell whether the\n  reported bug is this codepath vs something else — and even then say what\n  evidence would flip it, don't default to \"ask the author.\"\n\n`request-changes` is **not** a triage verdict. It is only for **trusted /\ninternal** authors with a **single tiny nit** under Approve — never for\n\"please rebuild this correctly.\"\n\n## Trusted / internal authors\n\nInfer from organization membership, team roles, prior maintainer behavior, or\nthe user explicitly identifying someone as a lead/internal contributor.\n\n| Who | Default |\n|-----|---------|\n| **#1 lead** | Prefer approve/merge. Don't supersede automatically. Never approve over their open review comments. |\n| **Other internal** | Prefer approve. One tiny nit → ask them (request-changes). Needs a real rewrite → **supersede** (or they bounce it to you). |\n| **External** | Normal bar. Idea right / shape wrong → **supersede** with credit. Never \"ask them to rebuild it.\" |\n\n## When to supersede\n\n- Contaminated / wrong close keywords\n- Stale conflicts, author not fixing\n- Duplicates a helper / wrong layer / wrong issue\n- Missing tests for the bug class while the idea is right\n- Same bug across 2+ PRs → one shared fix\n- Lead feedback ignored and the idea is still right\n\nNot enough alone: taste nits, optional follow-ups, \"could be prettier.\"\n\n## Supersede (only after go)\n\nThis is the default salvage ritual — not a rare escalation.\n\n1. **One** salvage worktree for the whole cluster — not one per PR.\n2. Keep the good idea; drop junk; reuse existing helpers (don't re-ask the\n   author to). Check what already landed on the default branch first — part of\n   the cluster may already be fixed, so build only the residual gaps.\n3. Credit **every** author in the cluster: cherry-pick or `Co-authored-by` +\n   `@handle` for each in the body.\n4. Tests for the bug class; green.\n5. Open **one** new PR/MR: `Supersedes #N, #M, …` for all members;\n   `Closes`/`Fixes` only for issues it really fixes.\n6. Close the cluster (below).\n7. Run `no-tropes` on public comments.\n\nTemplates: [reference.md](reference.md).\n\n## Close the cluster\n\nAfter supersede or a merge that kills siblings:\n\n1. Close every superseded/sibling PR/MR for that fix — comment `Superseded by #<new>.`, then close.\n2. Close related issues this work actually fixed (`Closes #` on the new PR, or close by hand).\n3. Don't close issues for a different bug class.\n4. \"Supersedes\" does not auto-close other PRs — you close them.\n5. Audit asks → page recent supersedes/merges, close stragglers, report counts.\n\n## Approve (after go)\n\n- Short approve (or merge if asked). Credit stays with the author.\n- Internal + **one tiny nit** → request-changes / ask them to fix first.\n- Anything bigger than a tiny nit → supersede, don't ball-bounce.\n- After merge, close leftover duplicate PRs/issues.\n\n## Before you stop (every item)\n\nRun this checklist at the end of each PR/item — don't wait to be reminded:\n\n- [ ] Batch clustered first: PRs sharing a fix/subsystem are grouped into ONE\n      super-PR, not enumerated as separate salvages.\n- [ ] Verdict is one of: approve / supersede / close-as-wrong-premise (no\n      \"keep open — ask author\" for reshape work).\n- [ ] If supersede: Next section has a concrete salvage plan (helpers, drops,\n      tests, credit for every author) — ready for \"go\".\n- [ ] Superseded/sibling PRs for this fix are closed with a pointer comment.\n- [ ] Related issues this actually fixed are closed (or `Closes #` on the new PR).\n- [ ] Any user-facing reply is short and actionable — an optional note, not an essay.\n- [ ] Shipping work is split into topical commits (`pr-update`) and CI is green.\n- [ ] You scoped the whole fix, not a timid subset.\n\n## Hard rules\n\n- Worktrees only for review work — not the primary checkout.\n- No supersede / close / approve / merge without explicit go.\n- No approve past unaddressed lead-maintainer comments.\n- No orphaned superseded PRs or fixed issues after a sweep.\n- No \"ask the author to rewrite\" when supersede criteria match — salvage it.", "en": "# PR Triage\n\nMaintainer triage. Three real outcomes: **approve**, **supersede** (salvage +\ncredit + close the old one), or **close** (wrong premise). Verdict first; wait\nfor an explicit **go** before forge writes (approve / supersede / close / merge /\npush). Use `gh` or `glab`.\n\n## Kickoff\n\nBatch (\"we will be reviewing…\") or a pasted Discord/`>>>…<<<` thread with a PR:\n\n1. Figure out repo + forge. Skim `AGENTS.md` / `CONTRIBUTING.md` if present.\n2. Match this repo's worktree/branch naming by inspecting existing worktrees.\n3. Don't touch the primary checkout — use worktrees.\n4. **Cluster the batch first** (below), then verdict + salvage plan; wait for go\n   before forge writes.\n\n## Cluster the batch first (before per-PR verdicts)\n\nMore than one PR/issue in front of you? Group them by the **fix they need**\nbefore you judge any single one. Multiple PRs on the same bug (or fragile\nsubsystem) are **one consolidation**, not N salvages.\n\n1. Map each PR/issue to the underlying fix and the files/subsystem it touches.\n2. Any two that fix the **same bug class or the same subsystem** → treat the\n   group as **one super-PR**: build the proper fix once, credit **every**\n   author (`Co-authored-by` + `@handle`), and supersede the whole cluster.\n3. Default to consolidation. Only keep PRs separate when the fixes are genuinely\n   independent (different bug class, different area, no shared code).\n4. State the grouping up front, e.g. `#68665 + #63590 + #67603 → one super-PR`,\n   then run the per-PR loop **within** each group to confirm premises.\n\nDo **not** enumerate one verdict per PR when they share a fix — that's the lazy\nshape. One cluster → one consolidated supersede.\n\n## Discord threads\n\nA pasted thread *is* the intake. Don't make them restate a bare URL.\n\n1. Pull out symptom, platform, linked PR/issue, what staff already said.\n2. Open those PRs; look for siblings on the same fix.\n3. Run the per-PR loop. Check the PR actually matches the reported bug.\n4. Draft a user reply only if useful — still wait for go before GH actions.\n5. No PR? Search open ones; if they want a new fix, follow that repository's\n   worktree workflow.\n\n## Forge cheatsheet\n\n| Action | GitHub | GitLab |\n|--------|--------|--------|\n| View | `gh pr view <N>` | `glab mr view <N>` |\n| Diff | `gh pr diff <N>` | `glab mr diff <N>` |\n| Comment | `gh pr comment <N>` | `glab mr note <N>` |\n| Review | `gh pr review <N> --approve \\| --request-changes` | `glab mr approve` / note |\n| Close PR/MR | `gh pr close <N>` | `glab mr close <N>` |\n| Close issue | `gh issue close <N>` | `glab issue close <N>` |\n\n```bash\ngit fetch origin pull/<N>/head:refs/remotes/origin/pr-<N>   # GitHub\n# GitLab: glab mr checkout <N>\n```\n\n## Per-PR loop\n\nRun this **within each cluster** (see \"Cluster the batch first\"). If the group\nconsolidates, the salvage plan is shared and you supersede all of them into one\nsuper-PR — don't emit a standalone salvage per member.\n\n1. Metadata + diff + existing reviews (`gh` / `glab`).\n2. Unaddressed **#1 / lead maintainer** comments → do not approve.\n3. Check the premise on current default branch (usually `main`).\n4. Cherry-pick/apply in a review worktree; run targeted tests this repo uses.\n5. Return a verdict:\n\n```markdown\n**PR/MR:** [#N](https://…/pull/N)\n\n**Verdict: approve | supersede | close-as-wrong-premise**\n\n**Worktree:** `…` · `<branch>`\n\n### Why\n- …\n\n### Checks\n| Check | Result |\n|---|---|\n| Merges onto default branch | … |\n| CI | … |\n| Targeted tests | … |\n\n### Next (after go)\n- approve: …\n- supersede: salvage plan (reuse X, drop Y, tests Z, credit @handle)\n- close: wrong-premise comment outline\n```\n\n## Pick the verdict (no soft exits)\n\n| Situation | Verdict |\n|-----------|---------|\n| Idea + shape are fine | **approve** |\n| Idea is right, shape is wrong (wrong helper/layer, missing tests for the bug class, contaminated, stale/conflicting, duplicate of another PR, ignored lead feedback) | **supersede** |\n| Premise doesn't hold on current default branch | **close-as-wrong-premise** |\n\n**Anti-patterns — do not do these:**\n\n- `keep open — ask the author to rewrite / reuse helper X / add coverage`\n  when the shape is already wrong → that **is** supersede. You salvage it.\n- Inventing a fourth outcome (`request-changes`, `needs-info`, `wait-and-see`)\n  for external PRs that need reshaping.\n- Emitting one verdict per PR when several fix the same bug/subsystem → that's\n  **one** consolidated super-PR that supersedes the cluster, credit all authors.\n- Parking on someone else's confirmation when the PR's *shape* is already a\n  supersede (wrong helper, wrong layer, no bug-class tests). Note the open\n  question under Why if useful; still verdict **supersede** with the salvage\n  plan. Only block the verdict when you literally cannot tell whether the\n  reported bug is this codepath vs something else — and even then say what\n  evidence would flip it, don't default to \"ask the author.\"\n\n`request-changes` is **not** a triage verdict. It is only for **trusted /\ninternal** authors with a **single tiny nit** under Approve — never for\n\"please rebuild this correctly.\"\n\n## Trusted / internal authors\n\nInfer from organization membership, team roles, prior maintainer behavior, or\nthe user explicitly identifying someone as a lead/internal contributor.\n\n| Who | Default |\n|-----|---------|\n| **#1 lead** | Prefer approve/merge. Don't supersede automatically. Never approve over their open review comments. |\n| **Other internal** | Prefer approve. One tiny nit → ask them (request-changes). Needs a real rewrite → **supersede** (or they bounce it to you). |\n| **External** | Normal bar. Idea right / shape wrong → **supersede** with credit. Never \"ask them to rebuild it.\" |\n\n## When to supersede\n\n- Contaminated / wrong close keywords\n- Stale conflicts, author not fixing\n- Duplicates a helper / wrong layer / wrong issue\n- Missing tests for the bug class while the idea is right\n- Same bug across 2+ PRs → one shared fix\n- Lead feedback ignored and the idea is still right\n\nNot enough alone: taste nits, optional follow-ups, \"could be prettier.\"\n\n## Supersede (only after go)\n\nThis is the default salvage ritual — not a rare escalation.\n\n1. **One** salvage worktree for the whole cluster — not one per PR.\n2. Keep the good idea; drop junk; reuse existing helpers (don't re-ask the\n   author to). Check what already landed on the default branch first — part of\n   the cluster may already be fixed, so build only the residual gaps.\n3. Credit **every** author in the cluster: cherry-pick or `Co-authored-by` +\n   `@handle` for each in the body.\n4. Tests for the bug class; green.\n5. Open **one** new PR/MR: `Supersedes #N, #M, …` for all members;\n   `Closes`/`Fixes` only for issues it really fixes.\n6. Close the cluster (below).\n7. Run `no-tropes` on public comments.\n\nTemplates: [reference.md](reference.md).\n\n## Close the cluster\n\nAfter supersede or a merge that kills siblings:\n\n1. Close every superseded/sibling PR/MR for that fix — comment `Superseded by #<new>.`, then close.\n2. Close related issues this work actually fixed (`Closes #` on the new PR, or close by hand).\n3. Don't close issues for a different bug class.\n4. \"Supersedes\" does not auto-close other PRs — you close them.\n5. Audit asks → page recent supersedes/merges, close stragglers, report counts.\n\n## Approve (after go)\n\n- Short approve (or merge if asked). Credit stays with the author.\n- Internal + **one tiny nit** → request-changes / ask them to fix first.\n- Anything bigger than a tiny nit → supersede, don't ball-bounce.\n- After merge, close leftover duplicate PRs/issues.\n\n## Before you stop (every item)\n\nRun this checklist at the end of each PR/item — don't wait to be reminded:\n\n- [ ] Batch clustered first: PRs sharing a fix/subsystem are grouped into ONE\n      super-PR, not enumerated as separate salvages.\n- [ ] Verdict is one of: approve / supersede / close-as-wrong-premise (no\n      \"keep open — ask author\" for reshape work).\n- [ ] If supersede: Next section has a concrete salvage plan (helpers, drops,\n      tests, credit for every author) — ready for \"go\".\n- [ ] Superseded/sibling PRs for this fix are closed with a pointer comment.\n- [ ] Related issues this actually fixed are closed (or `Closes #` on the new PR).\n- [ ] Any user-facing reply is short and actionable — an optional note, not an essay.\n- [ ] Shipping work is split into topical commits (`pr-update`) and CI is green.\n- [ ] You scoped the whole fix, not a timid subset.\n\n## Hard rules\n\n- Worktrees only for review work — not the primary checkout.\n- No supersede / close / approve / merge without explicit go.\n- No approve past unaddressed lead-maintainer comments.\n- No orphaned superseded PRs or fixed issues after a sweep.\n- No \"ask the author to rewrite\" when supersede criteria match — salvage it."}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /pr-triage.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /pr-triage.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /pr-triage pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /pr-triage protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["approve: …", "supersede: salvage plan (reuse X, drop Y, tests Z, credit @handle)", "close: wrong-premise comment outline", "`keep open — ask the author to rewrite / reuse helper X / add coverage`", "Inventing a fourth outcome (`request-changes`, `needs-info`, `wait-and-see`)"], "en": ["approve: …", "supersede: salvage plan (reuse X, drop Y, tests Z, credit @handle)", "close: wrong-premise comment outline", "`keep open — ask the author to rewrite / reuse helper X / add coverage`", "Inventing a fourth outcome (`request-changes`, `needs-info`, `wait-and-see`)"]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "clean", "no-tropes"], "sourcePath": "skills/pr-triage/SKILL.md"},
  {"name": "pr-update", "category": "pr-lifecycle", "invocation": "model", "description": {"id": "Skill autonomous engineering Brooklyn untuk /pr-update.", "en": "Brooklyn autonomous engineering skill for /pr-update."}, "detailedDescription": {"id": "# PR Update (open or refresh)\n\nOne skill for **create** and **update**. Make the title + body match the branch\nas it is now. Do not strip media from an existing description.\n\n## Steps\n\n1. `git status` / `git diff` / `git log` / `git diff <default>...HEAD` (and\n   remote tracking) so you know the full change set — not just the latest\n   commit.\n2. Detect an existing PR/MR for this branch (`gh pr view` / `glab mr view`).\n   - **None** → create (push upstream with `-u` if needed).\n   - **Exists** → edit title + body in place. Do not open a duplicate.\n3. Draft a title and body from **all** commits/files in the PR, not one commit.\n4. If updating: fetch the current body **first**. Preserve every media item\n   exactly (see below). Then rewrite text so Summary / Test plan (or the\n   repo's usual sections) stay accurate.\n5. Apply with `gh pr create` / `gh pr edit` (or `glab` equivalents).\n6. End with the PR/MR as a markdown link — the number **and** the full forge\n   URL (`[#123](https://github.com/org/repo/pull/123)`, GitLab `!` equivalent).\n   Never a bare `#123`. This is the one place the rule is written down; the\n   other skills just follow it.\n\n## Title + body\n\n- Title: concise, why-focused; match repo PR style when obvious.\n- Body: use the repo's template when one exists; otherwise:\n\n```markdown\n## Summary\n<1-3 bullets of what changed and why>\n\n## Test plan\n- [ ] <concrete checks>\n```\n\n- Run the prose through `no-tropes` before publishing.\n- Do not invent reviewers, labels, or milestone unless asked.\n\n## Commits\n\nShape the branch's commits as part of opening/refreshing the PR.\n\n- One concern per commit. Default to a topical split (2–5 is typical) even if\n  not asked; don't dump everything into one blob unless the user wants it.\n- Match the repo's recent commit style (`git log --oneline -15`); subject = why.\n  No \"WIP\" / \"fix stuff\".\n- Rebuild with soft reset + path-staged commits (no `git rebase -i` — needs a\n  TTY). Show the final `git log --oneline <default>..HEAD` before force-push.\n  Two traps in that recipe:\n  - Soft-reset to the branch's **real base SHA** (`git merge-base HEAD origin/<default>`),\n    never to `origin/<default>` itself — it moves mid-task and will stage other\n    people's commits as yours.\n  - `git reset <base> -- <path>` leaves that path **unstaged**, so the next\n    `git commit` silently captures its pre-edit version. Verify the content\n    landed (`git show <sha>:<path> | grep <new symbol>`) before force-push.\n- Keep authorship when reshaping others' work (`Co-authored-by` / cherry-pick),\n  especially during `pr-triage` salvage.\n- Don't mix pure formatting with logic, commit secrets/`.env`, or rewrite\n  commits already on the default branch without asking.\n\n## Preserve media (hard rule)\n\nWhen a PR/MR already has a body, **never delete media**.\n\nTreat as media (keep verbatim, same URLs/markup/order when possible):\n\n- Markdown images / links to images or video (`![](...)`, `[...](...)` to\n  media)\n- HTML `<img>`, `<video>`, `<source>`, `<br>` used between media blocks\n- GitHub/GitLab upload / user-attachments / moved-image URLs\n- Image/video-only paragraphs or HTML comments wrapping media\n\nUpdate flow:\n\n1. Read the existing body.\n2. Extract and keep the media blocks.\n3. Rewrite textual sections so they match the current diff.\n4. Re-attach the preserved media (same block(s), typically at the end unless\n   they were interleaved on purpose — then keep interleaving).\n5. Before submit, diff old body vs new: every media URL/markup from the old\n   body must still be present.\n\nIf a refresh would drop media, stop and keep the old body (or only edit the\ntitle) rather than publishing a media-stripped description.\n\n## Scope\n\n- **In:** open PR/MR, push if needed for create, shape commits, retitle, rewrite\n  description, preserve media, print URL.\n- **Out:** rebase, CI, and review threads (`pr-ready`), triage/salvage\n  (`pr-triage`), merging.\n\n## Forge cheatsheet\n\n```bash\n# GitHub — read\ngh pr view --json url,title,body,baseRefName,headRefName\n\n# GitHub — create\ngh pr create --title \"…\" --body \"$(cat <<'EOF'\n…\nEOF\n)\"\n\n# GitHub — update (body from file avoids shell-eating newlines/media)\ngh pr view --json body -q .body > /tmp/pr-body.md\n# …edit /tmp/pr-body.md carefully…\ngh pr edit --title \"…\" --body-file /tmp/pr-body.md\n\n# GitLab\nglab mr view\nglab mr create\nglab mr update <N> --title \"…\" --description-file /tmp/pr-body.md\n```", "en": "# PR Update (open or refresh)\n\nOne skill for **create** and **update**. Make the title + body match the branch\nas it is now. Do not strip media from an existing description.\n\n## Steps\n\n1. `git status` / `git diff` / `git log` / `git diff <default>...HEAD` (and\n   remote tracking) so you know the full change set — not just the latest\n   commit.\n2. Detect an existing PR/MR for this branch (`gh pr view` / `glab mr view`).\n   - **None** → create (push upstream with `-u` if needed).\n   - **Exists** → edit title + body in place. Do not open a duplicate.\n3. Draft a title and body from **all** commits/files in the PR, not one commit.\n4. If updating: fetch the current body **first**. Preserve every media item\n   exactly (see below). Then rewrite text so Summary / Test plan (or the\n   repo's usual sections) stay accurate.\n5. Apply with `gh pr create` / `gh pr edit` (or `glab` equivalents).\n6. End with the PR/MR as a markdown link — the number **and** the full forge\n   URL (`[#123](https://github.com/org/repo/pull/123)`, GitLab `!` equivalent).\n   Never a bare `#123`. This is the one place the rule is written down; the\n   other skills just follow it.\n\n## Title + body\n\n- Title: concise, why-focused; match repo PR style when obvious.\n- Body: use the repo's template when one exists; otherwise:\n\n```markdown\n## Summary\n<1-3 bullets of what changed and why>\n\n## Test plan\n- [ ] <concrete checks>\n```\n\n- Run the prose through `no-tropes` before publishing.\n- Do not invent reviewers, labels, or milestone unless asked.\n\n## Commits\n\nShape the branch's commits as part of opening/refreshing the PR.\n\n- One concern per commit. Default to a topical split (2–5 is typical) even if\n  not asked; don't dump everything into one blob unless the user wants it.\n- Match the repo's recent commit style (`git log --oneline -15`); subject = why.\n  No \"WIP\" / \"fix stuff\".\n- Rebuild with soft reset + path-staged commits (no `git rebase -i` — needs a\n  TTY). Show the final `git log --oneline <default>..HEAD` before force-push.\n  Two traps in that recipe:\n  - Soft-reset to the branch's **real base SHA** (`git merge-base HEAD origin/<default>`),\n    never to `origin/<default>` itself — it moves mid-task and will stage other\n    people's commits as yours.\n  - `git reset <base> -- <path>` leaves that path **unstaged**, so the next\n    `git commit` silently captures its pre-edit version. Verify the content\n    landed (`git show <sha>:<path> | grep <new symbol>`) before force-push.\n- Keep authorship when reshaping others' work (`Co-authored-by` / cherry-pick),\n  especially during `pr-triage` salvage.\n- Don't mix pure formatting with logic, commit secrets/`.env`, or rewrite\n  commits already on the default branch without asking.\n\n## Preserve media (hard rule)\n\nWhen a PR/MR already has a body, **never delete media**.\n\nTreat as media (keep verbatim, same URLs/markup/order when possible):\n\n- Markdown images / links to images or video (`![](...)`, `[...](...)` to\n  media)\n- HTML `<img>`, `<video>`, `<source>`, `<br>` used between media blocks\n- GitHub/GitLab upload / user-attachments / moved-image URLs\n- Image/video-only paragraphs or HTML comments wrapping media\n\nUpdate flow:\n\n1. Read the existing body.\n2. Extract and keep the media blocks.\n3. Rewrite textual sections so they match the current diff.\n4. Re-attach the preserved media (same block(s), typically at the end unless\n   they were interleaved on purpose — then keep interleaving).\n5. Before submit, diff old body vs new: every media URL/markup from the old\n   body must still be present.\n\nIf a refresh would drop media, stop and keep the old body (or only edit the\ntitle) rather than publishing a media-stripped description.\n\n## Scope\n\n- **In:** open PR/MR, push if needed for create, shape commits, retitle, rewrite\n  description, preserve media, print URL.\n- **Out:** rebase, CI, and review threads (`pr-ready`), triage/salvage\n  (`pr-triage`), merging.\n\n## Forge cheatsheet\n\n```bash\n# GitHub — read\ngh pr view --json url,title,body,baseRefName,headRefName\n\n# GitHub — create\ngh pr create --title \"…\" --body \"$(cat <<'EOF'\n…\nEOF\n)\"\n\n# GitHub — update (body from file avoids shell-eating newlines/media)\ngh pr view --json body -q .body > /tmp/pr-body.md\n# …edit /tmp/pr-body.md carefully…\ngh pr edit --title \"…\" --body-file /tmp/pr-body.md\n\n# GitLab\nglab mr view\nglab mr create\nglab mr update <N> --title \"…\" --description-file /tmp/pr-body.md\n```"}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /pr-update.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /pr-update.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /pr-update pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /pr-update protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["None** → create (push upstream with `-u` if needed).", "Exists** → edit title + body in place. Do not open a duplicate.", "Title: concise, why-focused; match repo PR style when obvious.", "Body: use the repo's template when one exists; otherwise:", "[ ] <concrete checks>"], "en": ["None** → create (push upstream with `-u` if needed).", "Exists** → edit title + body in place. Do not open a duplicate.", "Title: concise, why-focused; match repo PR style when obvious.", "Body: use the repo's template when one exists; otherwise:", "[ ] <concrete checks>"]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "clean", "no-tropes"], "sourcePath": "skills/pr-update/SKILL.md"},
  {"name": "research", "category": "system", "invocation": "model", "description": {"id": "Skill autonomous engineering Brooklyn untuk /research.", "en": "Brooklyn autonomous engineering skill for /research."}, "detailedDescription": {"id": "# Research\n\nTwo halves, in this order: the **documents of record** for this project, then\n**prior art** outside it. Answering from memory and calling it research is the\nthing this skill is here to stop.\n\nMatch the shape of the ask:\n\n- **A question** (\"is this possible\", \"are we doing this right\", \"don't touch\n  code\"): look it up, report, stop. No edits until told to go (→ `audit-only`).\n- **Part of the work** (\"research and fix\", \"sleuth GitHub and iterate until\n  it's right\"): research inside the loop. Look, apply, re-check, and say what\n  each pass was based on — don't halt the loop to file a report.\n\n## Pick a depth and say it\n\n| Depth | For | Shape |\n|---|---|---|\n| quick | one fact, one API, one flag | a source or two, answer in a paragraph |\n| medium | \"how do people do this\", choosing an approach | 3-6 sources, compare, recommend |\n| very thorough | architecture, redesign, \"this has to be solved somewhere\" | survey the field, name the canon, ROI-ordered options |\n\nFan out with parallel subagents for breadth. Give each one a scope, a\n`Thoroughness:` level, `Do not edit files.`, and a return contract: exact paths\nwith line numbers and short quotes for code, or URL, technique, license, and\napplicability for outside repos.\n\n## Documents of record first\n\nExhaust what the project already states before searching the web.\n\n- Tickets and specs: the tracker issue, the PRD or spec, QA feedback, the Slack\n  or Discord thread that started it — whatever this project treats as the\n  record.\n- The repo's own planning docs: `plans/`, `docs/`, ADRs, `AGENTS.md`,\n  `CONTEXT.md`, README.\n- Figma dev mode when the question is visual. The design is the source of truth\n  (→ `ui-system`).\n- The repo itself: `git log -S<symbol>` and `git log -p` for why the code got\n  this way, and the vendored source in `node_modules` or site-packages instead\n  of a remembered API surface.\n\nWhen two documents disagree, say so instead of silently picking one. \"Double\ncheck against X, Y, Z\" means re-read each one and answer per document, not\nre-skim your own diff.\n\n## Then prior art\n\nSomeone has almost certainly solved this. Go find them and take it.\n\n- GitHub: `gh search repos`, `gh search code`, `gh api`. Read the real source,\n  not the README. Note the license before recommending a lift.\n- arXiv and papers for anything on agents, generation, simulation, scheduling,\n  or scoring.\n- Vendor docs and changelogs for API truth. Tutorials, blogs, Reddit, HN, and\n  YouTube for what people actually hit in practice.\n\nReport what is reusable, which pattern to copy, and what to leave behind.\n\n## Grade the evidence\n\nRank the sources and mark which tier a claim sits in:\n\n1. Vendor-documented, or the shipping source code. Strongest.\n2. Maintainer statements, papers, primary repos.\n3. Blogs, tutorials, forum posts, model memory. Weakest, flag them as such.\n\nEvery non-obvious claim carries a URL. With no URL, write \"unverified\" rather\nthan stating it flat.\n\n## Don't\n\n- Answer from memory and call it research. \"Are you sure?\" and \"did you\n  research?\" mean you already got caught doing it.\n- Call something impossible or unsupported without opening the docs or source.\n- Stop at the first hit when the question was \"how do people do this\".\n- Hand back a link dump. The deliverable is the recommendation, with the\n  sources under it.\n- Drop the research on the floor. When it drives a change, the PR description\n  carries what was found and why (→ `pr-update`). When it's a big survey with\n  no PR yet, write it into the repo's planning docs so it outlives the chat.", "en": "# Research\n\nTwo halves, in this order: the **documents of record** for this project, then\n**prior art** outside it. Answering from memory and calling it research is the\nthing this skill is here to stop.\n\nMatch the shape of the ask:\n\n- **A question** (\"is this possible\", \"are we doing this right\", \"don't touch\n  code\"): look it up, report, stop. No edits until told to go (→ `audit-only`).\n- **Part of the work** (\"research and fix\", \"sleuth GitHub and iterate until\n  it's right\"): research inside the loop. Look, apply, re-check, and say what\n  each pass was based on — don't halt the loop to file a report.\n\n## Pick a depth and say it\n\n| Depth | For | Shape |\n|---|---|---|\n| quick | one fact, one API, one flag | a source or two, answer in a paragraph |\n| medium | \"how do people do this\", choosing an approach | 3-6 sources, compare, recommend |\n| very thorough | architecture, redesign, \"this has to be solved somewhere\" | survey the field, name the canon, ROI-ordered options |\n\nFan out with parallel subagents for breadth. Give each one a scope, a\n`Thoroughness:` level, `Do not edit files.`, and a return contract: exact paths\nwith line numbers and short quotes for code, or URL, technique, license, and\napplicability for outside repos.\n\n## Documents of record first\n\nExhaust what the project already states before searching the web.\n\n- Tickets and specs: the tracker issue, the PRD or spec, QA feedback, the Slack\n  or Discord thread that started it — whatever this project treats as the\n  record.\n- The repo's own planning docs: `plans/`, `docs/`, ADRs, `AGENTS.md`,\n  `CONTEXT.md`, README.\n- Figma dev mode when the question is visual. The design is the source of truth\n  (→ `ui-system`).\n- The repo itself: `git log -S<symbol>` and `git log -p` for why the code got\n  this way, and the vendored source in `node_modules` or site-packages instead\n  of a remembered API surface.\n\nWhen two documents disagree, say so instead of silently picking one. \"Double\ncheck against X, Y, Z\" means re-read each one and answer per document, not\nre-skim your own diff.\n\n## Then prior art\n\nSomeone has almost certainly solved this. Go find them and take it.\n\n- GitHub: `gh search repos`, `gh search code`, `gh api`. Read the real source,\n  not the README. Note the license before recommending a lift.\n- arXiv and papers for anything on agents, generation, simulation, scheduling,\n  or scoring.\n- Vendor docs and changelogs for API truth. Tutorials, blogs, Reddit, HN, and\n  YouTube for what people actually hit in practice.\n\nReport what is reusable, which pattern to copy, and what to leave behind.\n\n## Grade the evidence\n\nRank the sources and mark which tier a claim sits in:\n\n1. Vendor-documented, or the shipping source code. Strongest.\n2. Maintainer statements, papers, primary repos.\n3. Blogs, tutorials, forum posts, model memory. Weakest, flag them as such.\n\nEvery non-obvious claim carries a URL. With no URL, write \"unverified\" rather\nthan stating it flat.\n\n## Don't\n\n- Answer from memory and call it research. \"Are you sure?\" and \"did you\n  research?\" mean you already got caught doing it.\n- Call something impossible or unsupported without opening the docs or source.\n- Stop at the first hit when the question was \"how do people do this\".\n- Hand back a link dump. The deliverable is the recommendation, with the\n  sources under it.\n- Drop the research on the floor. When it drives a change, the PR description\n  carries what was found and why (→ `pr-update`). When it's a big survey with\n  no PR yet, write it into the repo's planning docs so it outlives the chat."}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /research.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /research.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /research pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /research protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["A question** (\"is this possible\", \"are we doing this right\", \"don't touch", "Part of the work** (\"research and fix\", \"sleuth GitHub and iterate until", "Tickets and specs: the tracker issue, the PRD or spec, QA feedback, the Slack", "The repo's own planning docs: `plans/`, `docs/`, ADRs, `AGENTS.md`,", "Figma dev mode when the question is visual. The design is the source of truth"], "en": ["A question** (\"is this possible\", \"are we doing this right\", \"don't touch", "Part of the work** (\"research and fix\", \"sleuth GitHub and iterate until", "Tickets and specs: the tracker issue, the PRD or spec, QA feedback, the Slack", "The repo's own planning docs: `plans/`, `docs/`, ADRs, `AGENTS.md`,", "Figma dev mode when the question is visual. The design is the source of truth"]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "clean", "no-tropes"], "sourcePath": "skills/research/SKILL.md"},
  {"name": "runtime-debug", "category": "quality", "invocation": "model", "description": {"id": "Skill autonomous engineering Brooklyn untuk /runtime-debug.", "en": "Brooklyn autonomous engineering skill for /runtime-debug."}, "detailedDescription": {"id": "# Runtime Debug\n\nWhen something breaks at runtime (a URL 500s, a deploy is down, an ephemeral env\nmisbehaves), look at what actually happened before editing code.\n\n## Steps\n\n1. Reproduce and capture the real failure (status, error, request id, time).\n2. Check observability first: logs, traces, metrics. Use an observability MCP\n   (e.g. Datadog) if one is available/connected; otherwise fall back to the\n   platform's log stream (CI job, container, Jenkins, cloud logs).\n3. Locate the failing service/line from the evidence, not a guess.\n4. Fix the real cause. Don't add logging-only \"fixes\" and call it done.\n5. For intentional lab/ephemeral config (test secrets, fail-open), don't raise\n   false alarms — leave required env in place.\n\n## Don't\n\n- Rewrite code speculatively before reading logs.\n- Report fixed without re-checking the runtime.", "en": "# Runtime Debug\n\nWhen something breaks at runtime (a URL 500s, a deploy is down, an ephemeral env\nmisbehaves), look at what actually happened before editing code.\n\n## Steps\n\n1. Reproduce and capture the real failure (status, error, request id, time).\n2. Check observability first: logs, traces, metrics. Use an observability MCP\n   (e.g. Datadog) if one is available/connected; otherwise fall back to the\n   platform's log stream (CI job, container, Jenkins, cloud logs).\n3. Locate the failing service/line from the evidence, not a guess.\n4. Fix the real cause. Don't add logging-only \"fixes\" and call it done.\n5. For intentional lab/ephemeral config (test secrets, fail-open), don't raise\n   false alarms — leave required env in place.\n\n## Don't\n\n- Rewrite code speculatively before reading logs.\n- Report fixed without re-checking the runtime."}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /runtime-debug.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /runtime-debug.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /runtime-debug pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /runtime-debug protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["Rewrite code speculatively before reading logs.", "Report fixed without re-checking the runtime."], "en": ["Rewrite code speculatively before reading logs.", "Report fixed without re-checking the runtime."]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "clean", "no-tropes"], "sourcePath": "skills/runtime-debug/SKILL.md"},
  {"name": "stacked-pr", "category": "pr-lifecycle", "invocation": "user", "description": {"id": "Skill autonomous engineering Brooklyn untuk /stacked-pr.", "en": "Brooklyn autonomous engineering skill for /stacked-pr."}, "detailedDescription": {"id": "# Stacked & Multi-base PRs\n\n## Stacked (dependent) PRs\n\nA child PR builds on a parent PR's branch. Update the stack bottom-up:\n\n1. Update the parent branch/PR first.\n2. Rebase the child onto the updated parent — not onto the default branch, and\n   never \"merge main into the leaf\" as the only step.\n3. Push each level; keep each PR's base pointing at the one below it.\n\n## Multi-base (release + default)\n\nSome orgs require the same change on the default branch AND a release/production\nbranch:\n\n1. Confirm the required target branches from the repo's rules.\n2. Land the change once, then cherry-pick/port the identical code to the other\n   base as a second PR.\n3. Keep the two PRs' code identical; note the sibling in each description.\n\n## Don't\n\n- Collapse a stack by merging the default branch into the child.\n- Let the two base PRs drift apart.\n- Finish without listing every PR/MR in the stack as a link.", "en": "# Stacked & Multi-base PRs\n\n## Stacked (dependent) PRs\n\nA child PR builds on a parent PR's branch. Update the stack bottom-up:\n\n1. Update the parent branch/PR first.\n2. Rebase the child onto the updated parent — not onto the default branch, and\n   never \"merge main into the leaf\" as the only step.\n3. Push each level; keep each PR's base pointing at the one below it.\n\n## Multi-base (release + default)\n\nSome orgs require the same change on the default branch AND a release/production\nbranch:\n\n1. Confirm the required target branches from the repo's rules.\n2. Land the change once, then cherry-pick/port the identical code to the other\n   base as a second PR.\n3. Keep the two PRs' code identical; note the sibling in each description.\n\n## Don't\n\n- Collapse a stack by merging the default branch into the child.\n- Let the two base PRs drift apart.\n- Finish without listing every PR/MR in the stack as a link."}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /stacked-pr.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /stacked-pr.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /stacked-pr pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /stacked-pr protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["Collapse a stack by merging the default branch into the child.", "Let the two base PRs drift apart.", "Finish without listing every PR/MR in the stack as a link."], "en": ["Collapse a stack by merging the default branch into the child.", "Let the two base PRs drift apart.", "Finish without listing every PR/MR in the stack as a link."]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "clean", "no-tropes"], "sourcePath": "skills/stacked-pr/SKILL.md"},
  {"name": "ticket-ship", "category": "pr-lifecycle", "invocation": "model", "description": {"id": "Skill autonomous engineering Brooklyn untuk /ticket-ship.", "en": "Brooklyn autonomous engineering skill for /ticket-ship."}, "detailedDescription": {"id": "# Ticket → Ship\n\nThe ticket-shaped parts of shipping. The PR mechanics live in their own skills —\nthis is what a tracker adds on top.\n\n## Steps\n\n1. Read the ticket and list the assigned work (`acli`, Jira, Linear, etc.).\n2. Confirm the correct base branch(es) before starting — some orgs need a\n   release branch plus the default (`stacked-pr`). Never assume; verify the\n   org's rule.\n3. Find the existing PR or start one (`work` → `pr-update`), then drive it\n   mergeable (`pr-ready`) and polish before handoff (`clean`).\n4. Move the ticket to the right state (In Review / Test / whatever this tracker\n   uses). Link the PR on the ticket.\n5. Draft the stakeholder update — Slack or a ticket comment, short, `no-tropes`.\n   Don't send it unless asked.\n\n## Don't\n\n- Assume the base branch.\n- Mark the ticket done while CI is red or review threads are open.\n- Let the ticket and the PR tell different stories about the state of the work.", "en": "# Ticket → Ship\n\nThe ticket-shaped parts of shipping. The PR mechanics live in their own skills —\nthis is what a tracker adds on top.\n\n## Steps\n\n1. Read the ticket and list the assigned work (`acli`, Jira, Linear, etc.).\n2. Confirm the correct base branch(es) before starting — some orgs need a\n   release branch plus the default (`stacked-pr`). Never assume; verify the\n   org's rule.\n3. Find the existing PR or start one (`work` → `pr-update`), then drive it\n   mergeable (`pr-ready`) and polish before handoff (`clean`).\n4. Move the ticket to the right state (In Review / Test / whatever this tracker\n   uses). Link the PR on the ticket.\n5. Draft the stakeholder update — Slack or a ticket comment, short, `no-tropes`.\n   Don't send it unless asked.\n\n## Don't\n\n- Assume the base branch.\n- Mark the ticket done while CI is red or review threads are open.\n- Let the ticket and the PR tell different stories about the state of the work."}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /ticket-ship.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /ticket-ship.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /ticket-ship pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /ticket-ship protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["Assume the base branch.", "Mark the ticket done while CI is red or review threads are open.", "Let the ticket and the PR tell different stories about the state of the work."], "en": ["Assume the base branch.", "Mark the ticket done while CI is red or review threads are open.", "Let the ticket and the PR tell different stories about the state of the work."]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "clean", "no-tropes"], "sourcePath": "skills/ticket-ship/SKILL.md"},
  {"name": "ui-only", "category": "system", "invocation": "model", "description": {"id": "Skill autonomous engineering Brooklyn untuk /ui-only.", "en": "Brooklyn autonomous engineering skill for /ui-only."}, "detailedDescription": {"id": "# UI Only\n\nWhen you're changing how something looks or interacts: get the UI right first.\nDon't burn turns on tsc, lint, or commit until they say it's good.\n\n## Don't run yet\n\nUntil they like the UI (or explicitly say to clean/commit):\n\n- `tsc` / typecheck / `npm run build` as a finish step\n- eslint / prettier / biome\n- full test suites just to greenlight a commit\n- `git commit` / `git push` / opening the PR as done\n\nDev server, screenshots, and targeted runs needed to see or use the UI are fine.\nDon't start a dev server the user already has running — assume it's up unless\ntold otherwise. Keep debug logging in place while something's still broken;\nremove it only once it works.\n\n## Do this instead\n\n1. Change the UI.\n2. Show it (or tell them how to look).\n3. Take feedback and iterate.\n4. When they say it's good / lgtm / clean it / commit it, then run checks,\n   use `clean`, commit, and do whatever else the repo needs.\n\nIf you're not sure whether to keep iterating, ask.\n\n## Scope\n\nAny UI surface (web, desktop, TUI). Skip for backend-only work.\nFor primitives and tokens, also use `ui-system`.", "en": "# UI Only\n\nWhen you're changing how something looks or interacts: get the UI right first.\nDon't burn turns on tsc, lint, or commit until they say it's good.\n\n## Don't run yet\n\nUntil they like the UI (or explicitly say to clean/commit):\n\n- `tsc` / typecheck / `npm run build` as a finish step\n- eslint / prettier / biome\n- full test suites just to greenlight a commit\n- `git commit` / `git push` / opening the PR as done\n\nDev server, screenshots, and targeted runs needed to see or use the UI are fine.\nDon't start a dev server the user already has running — assume it's up unless\ntold otherwise. Keep debug logging in place while something's still broken;\nremove it only once it works.\n\n## Do this instead\n\n1. Change the UI.\n2. Show it (or tell them how to look).\n3. Take feedback and iterate.\n4. When they say it's good / lgtm / clean it / commit it, then run checks,\n   use `clean`, commit, and do whatever else the repo needs.\n\nIf you're not sure whether to keep iterating, ask.\n\n## Scope\n\nAny UI surface (web, desktop, TUI). Skip for backend-only work.\nFor primitives and tokens, also use `ui-system`."}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /ui-only.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /ui-only.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /ui-only pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /ui-only protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["`tsc` / typecheck / `npm run build` as a finish step", "eslint / prettier / biome", "full test suites just to greenlight a commit", "`git commit` / `git push` / opening the PR as done"], "en": ["`tsc` / typecheck / `npm run build` as a finish step", "eslint / prettier / biome", "full test suites just to greenlight a commit", "`git commit` / `git push` / opening the PR as done"]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "clean", "no-tropes"], "sourcePath": "skills/ui-only/SKILL.md"},
  {"name": "ui-system", "category": "system", "invocation": "model", "description": {"id": "Skill autonomous engineering Brooklyn untuk /ui-system.", "en": "Brooklyn autonomous engineering skill for /ui-system."}, "detailedDescription": {"id": "# UI System\n\nReuse what the app already has. Don't invent a parallel UI kit.\n\n## Do\n\n1. Read `DESIGN.md` / area docs for the surface you're in.\n2. Find the existing component/helper (button, input, dialog, progress,\n   relative-time, dropdown, back row, …). Copy how Settings / Projects / etc.\n   already do it.\n3. Prefer a **variant** on an existing primitive over a new component.\n4. Colors/spacing/type from **existing CSS vars / tokens** (including the\n   app's Tailwind v4 setup). No one-off hex/shadow stacks.\n5. Match the app's look: if the app doesn't use borders/shadows/sparkles,\n   neither does your feature.\n\n## Don't\n\n- New button/input/modal implementations\n- Local color palettes or decorative chrome\n- Duplicate formatters (time, etc.) — use the shared ones\n- New borders, backgrounds, shadows, sparkles, or badges the surrounding app\n  doesn't already use — match the neighboring surface exactly\n- Section chrome that nowhere else uses\n\n## Working from a design\n\nFigma file, mock, or screenshot: match it — spacing, contrast, placement —\nbefore inventing anything.\n\n1. Pull the design context (Figma MCP if available, else the screenshot/mock).\n2. Map it to existing primitives and tokens before building anything new.\n3. Layout and structure first, then content.\n4. Match spacing, padding, contrast, and states exactly. Check the details —\n   no white-on-white, no doubled padding, correct CTA contrast.\n5. Optimize exported assets (SVGO for SVGs).\n6. Don't approximate spacing or colors the design specifies, and don't add\n   chrome neither the design nor the app has.\n\nIf you can't find the primitive, search harder or ask — don't quietly make one.\n\nWhile still iterating on look, follow `ui-only` (no tsc/lint/commit yet). Once\nthey like it, confirm against the design with `visual-verify`.", "en": "# UI System\n\nReuse what the app already has. Don't invent a parallel UI kit.\n\n## Do\n\n1. Read `DESIGN.md` / area docs for the surface you're in.\n2. Find the existing component/helper (button, input, dialog, progress,\n   relative-time, dropdown, back row, …). Copy how Settings / Projects / etc.\n   already do it.\n3. Prefer a **variant** on an existing primitive over a new component.\n4. Colors/spacing/type from **existing CSS vars / tokens** (including the\n   app's Tailwind v4 setup). No one-off hex/shadow stacks.\n5. Match the app's look: if the app doesn't use borders/shadows/sparkles,\n   neither does your feature.\n\n## Don't\n\n- New button/input/modal implementations\n- Local color palettes or decorative chrome\n- Duplicate formatters (time, etc.) — use the shared ones\n- New borders, backgrounds, shadows, sparkles, or badges the surrounding app\n  doesn't already use — match the neighboring surface exactly\n- Section chrome that nowhere else uses\n\n## Working from a design\n\nFigma file, mock, or screenshot: match it — spacing, contrast, placement —\nbefore inventing anything.\n\n1. Pull the design context (Figma MCP if available, else the screenshot/mock).\n2. Map it to existing primitives and tokens before building anything new.\n3. Layout and structure first, then content.\n4. Match spacing, padding, contrast, and states exactly. Check the details —\n   no white-on-white, no doubled padding, correct CTA contrast.\n5. Optimize exported assets (SVGO for SVGs).\n6. Don't approximate spacing or colors the design specifies, and don't add\n   chrome neither the design nor the app has.\n\nIf you can't find the primitive, search harder or ask — don't quietly make one.\n\nWhile still iterating on look, follow `ui-only` (no tsc/lint/commit yet). Once\nthey like it, confirm against the design with `visual-verify`."}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /ui-system.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /ui-system.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /ui-system pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /ui-system protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["New button/input/modal implementations", "Local color palettes or decorative chrome", "Duplicate formatters (time, etc.) — use the shared ones", "New borders, backgrounds, shadows, sparkles, or badges the surrounding app", "Section chrome that nowhere else uses"], "en": ["New button/input/modal implementations", "Local color palettes or decorative chrome", "Duplicate formatters (time, etc.) — use the shared ones", "New borders, backgrounds, shadows, sparkles, or badges the surrounding app", "Section chrome that nowhere else uses"]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "clean", "no-tropes"], "sourcePath": "skills/ui-system/SKILL.md"},
  {"name": "visual-verify", "category": "quality", "invocation": "model", "description": {"id": "Skill autonomous engineering Brooklyn untuk /visual-verify.", "en": "Brooklyn autonomous engineering skill for /visual-verify."}, "detailedDescription": {"id": "# Visual Verify\n\nNever report a visual, layout, or theme change as done based on the diff or a\npassing test. Look at the running surface.\n\n## Steps\n\n1. Run the actual surface (dev server, app, TUI/GUI, Storybook — whatever shows\n   this UI). Don't start one the user already has running.\n2. Capture what it looks like — screenshot it, or tell the user exactly how to\n   see it.\n3. Compare against the reference: the design (Figma/mock), the sibling surface it\n   should match, or the before state. Name the specific things you checked\n   (color values, spacing, border, active state, contrast).\n4. If it doesn't match, keep the debug logging and iterate. Only say \"done\" once\n   it visibly matches.\n\n## Don't\n\n- Claim a theme/color change works because the token math looks right — verify\n  the rendered value.\n- Delete instrumentation while the visual is still wrong.\n- Trust unit tests as proof a UI renders correctly.", "en": "# Visual Verify\n\nNever report a visual, layout, or theme change as done based on the diff or a\npassing test. Look at the running surface.\n\n## Steps\n\n1. Run the actual surface (dev server, app, TUI/GUI, Storybook — whatever shows\n   this UI). Don't start one the user already has running.\n2. Capture what it looks like — screenshot it, or tell the user exactly how to\n   see it.\n3. Compare against the reference: the design (Figma/mock), the sibling surface it\n   should match, or the before state. Name the specific things you checked\n   (color values, spacing, border, active state, contrast).\n4. If it doesn't match, keep the debug logging and iterate. Only say \"done\" once\n   it visibly matches.\n\n## Don't\n\n- Claim a theme/color change works because the token math looks right — verify\n  the rendered value.\n- Delete instrumentation while the visual is still wrong.\n- Trust unit tests as proof a UI renders correctly."}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /visual-verify.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /visual-verify.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /visual-verify pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /visual-verify protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["Claim a theme/color change works because the token math looks right — verify", "Delete instrumentation while the visual is still wrong.", "Trust unit tests as proof a UI renders correctly."], "en": ["Claim a theme/color change works because the token math looks right — verify", "Delete instrumentation while the visual is still wrong.", "Trust unit tests as proof a UI renders correctly."]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "clean", "no-tropes"], "sourcePath": "skills/visual-verify/SKILL.md"},
  {"name": "work", "category": "pr-lifecycle", "invocation": "user", "description": {"id": "Skill autonomous engineering Brooklyn untuk /work.", "en": "Brooklyn autonomous engineering skill for /work."}, "detailedDescription": {"id": "# /work\n\n`/work [slug]` is a declaration for this task: \"spin up a dedicated worktree and\ndo the work there.\" It applies to what the user is starting right now — it is\nnot a standing rule that all work always happens in worktrees.\n\nWhen invoked:\n\n1. Look at what the repo already does, and copy it.\n2. Create the worktree + branch and `cd` into it.\n3. Do the requested work there.\n\n## Read the repo's conventions\n\n```bash\ngit worktree list                                   # sibling dirs + branches\ngit branch -a --sort=-committerdate | head -20      # branch prefix/slug pattern\ngit remote show origin | sed -n 's/.*HEAD branch: //p'   # default branch\n```\n\n- **Branch prefix** — mirror the dominant one (e.g. `<initials>/<slug>`). None\n  obvious → ask.\n- **Worktree dir** — mirror sibling naming/location (commonly `<repo>-<short>`\n  next to the primary checkout). Short suffix ≠ full branch is fine.\n- **Base** — current default branch unless they name another.\n- **Slug** — from the `/work` argument, else the task description.\n\n## Create it\n\n```bash\nrepo=\"$(git rev-parse --show-toplevel)\"\nparent=\"$(dirname \"$repo\")\"\ndefault=\"$(git -C \"$repo\" remote show origin | sed -n 's/.*HEAD branch: //p')\"\ngit -C \"$repo\" fetch origin \"$default\"\ngit -C \"$repo\" worktree add -b <prefix>/<slug> \"$parent/$(basename \"$repo\")-<short>\" \"origin/$default\"\ncd \"$parent/$(basename \"$repo\")-<short>\"\n```\n\nReuse a matching worktree/branch if one already exists — don't duplicate. Match\nhow siblings handle `node_modules` / `.venv` / symlinks.\n\n## Then\n\n- Do the task in the worktree; never in the primary checkout, and never in\n  another agent's worktree.\n- UI → `ui-only` + `ui-system`. Tests → whatever the repo uses.\n- When handing off, tell the user the worktree path and how to run it.\n- Ship → commit, push the branch, open a PR/MR to the default branch.\n- Cleaning up → offer to remove worktrees whose branch is already merged.\n- Reviewing an existing PR instead of new work → `pr-triage`.", "en": "# /work\n\n`/work [slug]` is a declaration for this task: \"spin up a dedicated worktree and\ndo the work there.\" It applies to what the user is starting right now — it is\nnot a standing rule that all work always happens in worktrees.\n\nWhen invoked:\n\n1. Look at what the repo already does, and copy it.\n2. Create the worktree + branch and `cd` into it.\n3. Do the requested work there.\n\n## Read the repo's conventions\n\n```bash\ngit worktree list                                   # sibling dirs + branches\ngit branch -a --sort=-committerdate | head -20      # branch prefix/slug pattern\ngit remote show origin | sed -n 's/.*HEAD branch: //p'   # default branch\n```\n\n- **Branch prefix** — mirror the dominant one (e.g. `<initials>/<slug>`). None\n  obvious → ask.\n- **Worktree dir** — mirror sibling naming/location (commonly `<repo>-<short>`\n  next to the primary checkout). Short suffix ≠ full branch is fine.\n- **Base** — current default branch unless they name another.\n- **Slug** — from the `/work` argument, else the task description.\n\n## Create it\n\n```bash\nrepo=\"$(git rev-parse --show-toplevel)\"\nparent=\"$(dirname \"$repo\")\"\ndefault=\"$(git -C \"$repo\" remote show origin | sed -n 's/.*HEAD branch: //p')\"\ngit -C \"$repo\" fetch origin \"$default\"\ngit -C \"$repo\" worktree add -b <prefix>/<slug> \"$parent/$(basename \"$repo\")-<short>\" \"origin/$default\"\ncd \"$parent/$(basename \"$repo\")-<short>\"\n```\n\nReuse a matching worktree/branch if one already exists — don't duplicate. Match\nhow siblings handle `node_modules` / `.venv` / symlinks.\n\n## Then\n\n- Do the task in the worktree; never in the primary checkout, and never in\n  another agent's worktree.\n- UI → `ui-only` + `ui-system`. Tests → whatever the repo uses.\n- When handing off, tell the user the worktree path and how to run it.\n- Ship → commit, push the branch, open a PR/MR to the default branch.\n- Cleaning up → offer to remove worktrees whose branch is already merged.\n- Reviewing an existing PR instead of new work → `pr-triage`."}, "useWhen": {"id": ["Menjalankan alur kerja otomatis untuk /work.", "Memastikan git diff dan PR description bersih dari artefak AI."], "en": ["Executing automated engineering workflows for /work.", "Keeping git diffs, commits, and PR descriptions free of AI artifacts."]}, "avoidWhen": {"id": ["Perubahan kode masih eksperimental dan belum siap di-review manusia."], "en": ["Work is purely exploratory and not yet intended for human review."]}, "howItWorks": {"id": ["Agent menginisialisasi protokol /work pada branch aktif.", "Menganalisis status git, diff staging, dan log eksekusi.", "Menerapkan perbaikan atau pembersihan secara atomik.", "Melaporkan ringkasan state final ke reviewer."], "en": ["The agent initializes the /work protocol on the active branch.", "Analyzes git status, staging diffs, and test output.", "Applies clean, atomic refactors or publishes the pull request.", "Reports state transitions concisely without noisy heartbeats."]}, "coreRules": {"id": ["Branch prefix** — mirror the dominant one (e.g. `<initials>/<slug>`). None", "Worktree dir** — mirror sibling naming/location (commonly `<repo>-<short>`", "Base** — current default branch unless they name another.", "Slug** — from the `/work` argument, else the task description.", "Do the task in the worktree; never in the primary checkout, and never in"], "en": ["Branch prefix** — mirror the dominant one (e.g. `<initials>/<slug>`). None", "Worktree dir** — mirror sibling naming/location (commonly `<repo>-<short>`", "Base** — current default branch unless they name another.", "Slug** — from the `/work` argument, else the task description.", "Do the task in the worktree; never in the primary checkout, and never in"]}, "tips": {"id": ["Gunakan /work untuk membuat worktree terpisah agar branch utama tetap bersih.", "Jalankan /no-tropes sebelum submit PR untuk memangkas kata-kata klise."], "en": ["Use /work to spawn dedicated worktrees and keep your main branch pristine.", "Run /no-tropes before handoff to strip synthetic AI buzzwords from descriptions."]}, "pairsWellWith": ["cpr", "clean", "no-tropes"], "sourcePath": "skills/work/SKILL.md"},
];
